# HistoryAurora 验证合同

## 1.25.0 运行态与控制台「类」验证（REQ-UI-124 / 125）

- `StatefulButtonContractTests.ButtonTemplatesTakeRunningStateFromFrameworkOwnedItemStatusOnly`：
  `Aurora.Button.Base` 与 `Aurora.Panel.Switch` 的运行态触发器是 `AutomationProperties.ItemStatus`，
  值为「运行中」且显示 `RunningProgress`；两份样式的 Setter 与模板里一条 `AuroraCommandActivity` 都不剩。
- `StatefulButtonContractTests.ActivityPushesItemStatusOntoEveryAttachedElementAndReleasesReplacedOnes`：
  挂上写「就绪」、运行中写「运行中」、结束写「已完成」；改挂另一份状态的元素当场回到「就绪」。
- `ShellChromeContractTests.ConsoleToolbarShowsLevelAndDomainOnlyAndDropsClass`：控制台工具条不再有 `ClassFilter`。
- 2026-09-19 实测：Release 构建 0 警告 0 错误；完整 Contracts **377/377 通过**（含新增 2 条、改写 1 条）。
- **待人手验（只能真机）：** 热装本版后**重启一次宿主**，再在 Janus 项目操作面板点一个按钮——
  按钮周边应出现走动的虚线环与右侧「运行中」角标，期间点不动；命令结束后环消失、按钮恢复。
  再热重载任意一个模块，重复同一步，指示必须照常出现。控制台工具条应只剩「级别」「域」两段。

## 1.24.0 content 弹窗动作候选验证（REQ-UI-123）

- `AuroraDialogContractTests.ContentWithOptionsShowsBothTheBodyAndTheActionList`：正文、摘要、候选表、
  一对按钮同时在；默认选中第一项，改选后 `SelectedChoiceValue` 跟着变；主按钮文案取 `primary`。
- `AuroraDialogContractTests.ContentWithoutOptionsStaysACloseOnlyPreview`：不给 `options` 的 `content`
  没有候选表、没有取消键、主按钮仍是「关闭」——旧调用方零影响。
- 2026-09-18 实测：Release 构建 0 警告 0 错误；完整 Contracts **375/375 通过**（含新增 2 条）；质量门禁通过。
- **待人手验：** 弹窗要真鼠标。须在 Janus 项目总览点「提交」核对：窗里上半是可滚动的 diff、
  下半是「提交 / 删除本次脏工作树」两项，双击候选项等同于按主按钮，按取消或右上角 × 都不执行任何一项。

## 1.23.0 控制台宿主唯一日志验证（REQ-UI-121）

- `ConsoleLogSnapshotContractTests.HostBackedLogWritesThroughAndShowsEveryHostEntryOnce`：补读、界面写入、宿主写入各显示一次且序号连续；
  界面写入转进宿主并已脱敏；`Dispose` 后不再收到。
- `ConsoleLogSnapshotContractTests.InProcessConsoleShowsTheHostLogInsteadOfItsOwn`：`AuroraShellHost` 以宿主日志建控制台、
  不再 `new MemoryShellLog()`，Shutdown 解除订阅。
- 编译针对 HistoryVulcan 5.5.0 正式快照；须宿主 5.5.0 先部署。真机须核对：在 Minerva 打包查品牌时控制台逐轮出现
  `cmd:progress:apollo:chat` 与 `cmd:progress:minerva:conversion`，界面触发的命令回显只出现一次。

## 1.22 独立浮窗退役与测试修剪验证（REQ-UI-120）

- 新增 `ShellChromeContractTests.FloatingWindowsAreNotAUserSurface`：`IDockingService` 无 `Float`、快照无 `FloatingWindows`、
  `DockContentSnapshot` 只剩 `Id`；`aurora.ui.float` / `aurora.ui.floatstate` 执行失败；视图菜单无「浮动」。
- `EveryPaneRoutesPointerInputToThePageDragCoordinator` 改判窗格样式上只挂按下一条事件。
- 「一格一页」「落空隐藏」「侧边页拖进中央区」三条停靠用例改用 internal `DockingHost.FloatForDrag` 模拟拖动载体，原样通过；
  `JsonSnapshotPreservesNestedSplitsTabsAndSelection`（原 `…AndMultipleFloatingWindows`）去掉浮窗部分并断言快照不含 floating。
- 删除用例 15 条：`ShellChromeContractTests` 浮窗 6 条、`FloatingCenterPageRoundTripsWithoutInvalidatingMainLayout`、
  `MaximizeContractTests` 浮窗 2 条、`PageDragGestureTests` 窗口拖动会话 / 最大化双倍阈值 / 浮窗最大化切换（Theory 3 例）共 5 条、
  `FloatingDockMouseSmoke`（连同 `MouseInput`）。删除 `Smoke` 工程（1 条占位）。检查「顶栏已删干净」的守卫用例全部保留。
- 2026-09-14 实测：Release 构建 0 警告 0 错误；`Contracts` **364/364**（378 − 15 + 1）、`ModuleSmoke` **4/4** 通过；质量门禁通过。
- 2026-09-14 部署：包经 `-OutputRoot` 出到临时目录，`vulcan.module.install` 热装到宿主 5.4.0（PID 3928）：
  attached=true、attachFailures 为空、commandCount **61**（原 63，少 `aurora.ui.float` / `floatstate`）。宿主 shell 日志
  热装前后无 Aurora 报错。Diana 捕获 1279×836，SHA-256 `8216019BDBABA50A21007D306275079D23C2B9277EBED012C25943D571D47665`：
  Janus 场景四格布局完整，8 / 8 / 16 边距圆角保留。
- **待人手验：** 拖动要真鼠标，本轮未测。须核对 Ctrl 标签拖页面、右栏胶囊拖出仍出蓝色停靠点，落到停靠点进那一格、落空隐藏；
  视图菜单每页只剩 显示 / 隐藏 / 复位。

## 1.23.1 页面放宽与实线分隔验证（REQ-UI-122）

- `PageComponentsContractTests.ComponentGap_EqualsThePageInsetForEveryGapKeyword`：stack（tight / normal / 缺省）
  与 grid（tight / normal）的组件间距都等于 `PageRegistrar.PagePad`；`ComponentGap_NoneStaysZero`：none 仍为 0。
- `ShellChromeContractTests.SegmentDividerIsASolidHairlineLikeTheTable`：分段竖线无 OpacityMask、宽 1、对齐设备像素。
- `PageCornerIsConcentricWithComponentCorner` 以新值（20 = 8 + 12）照旧通过；面板像素用例
  `HorizontalPanelActuallyPaintsItsSeparators` 照旧通过。
- 2026-09-17 实测：完整 Contracts **373/373 通过**（含新增 7 条）。

## 1.22 页面新风格验证（REQ-UI-119）

- `ShellChromeContractTests.PageCornerIsConcentricWithComponentCorner`：浅色、深色下 `Radius.Page = Radius.Inner + PagePad`，
  组件圆角小于页面圆角，令牌 `Aurora.Space.PageInset` 等于 `PageRegistrar.PageInset`。
- `ShellChromeContractTests.ConsoleUsesTheSharedPageInset`：控制台 Padding 取共享内边距、裁切，根 DockPanel 无自带 Margin。
- `LabelModeCoverKeepsTheCardCorners` 改判 `Aurora.Radius.Page`；`ModulePageLoaderContractTests` 与
  `PageScrollContractTests.TheInsetClipsInsteadOfOverflowing` 改判共享常量，不再各记一个 12。
- 2026-09-14 实测：完整 Contracts **378/378 通过**（新增 2 条）；质量门禁通过。
- 2026-09-14 真机：包经 `-OutputRoot` 出到临时目录（不覆盖 z-Publish 的 1.22.0 正式快照），
  `vulcan.module.install` 热装到宿主 5.4.0（PID 20536）：attached=true、commandCount=63、attachFailures 为空。
  改前 Diana 捕获控制台工具条距卡片 10/8px、其它页 12px；改后捕获 1279×836，
  SHA-256 `2699D9AFDF5290AD7B9F54DD32A59B4061C64DCD41D69379DE89A7B0662D392D`：控制面板、表格工具条、控制台工具条
  距卡片均为 6px，放大角部确认卡片圆角与组件圆角同心。**只验了深色主题**，浅色未真机看。
- 同日第二轮（用户反馈 6 / 4 / 10 组件圆角过小发僵）：改为内边距 8、组件圆角 8、页面圆角 16。
  Contracts **378/378 通过**，质量门禁通过，再次热装 attached=true、commandCount=63。捕获 1279×836，
  SHA-256 `C59A008989FE85952B284A16E6AEE9D9CB3582C589508E86A7748DDA4D8CCCF7`：三处贴角组件距卡片 8px，
  角部放大确认同心。仍只验深色主题。

## 1.22.0 验证

- `AuroraDialogContractTests.InProcessShellClaimsHostConfirmationChannel`：`AuroraFrontend` 使用
  `MessageBoxConfirmation`，`AuroraBusinessComposition` 经 `RegisterFrontend` 登记，模块源码不再写宿主总线开关。
- `ShellChromeContractTests.ThemeChoiceSurvivesSettingsAndWindowRecreation`、`ConsoleHistoryRedactionTests`：
  迁入的 `JsonSettingsStore` 与 `CommandHistory` 读写和旧文件格式兼容。
- `DockingContractTests`：删除 `ShellWindow.Modules` 后界面注册表断言不变。
- 编译针对 HistoryVulcan 5.4.0 宿主运行库（`-p:HistoryVulcanHostRoot=<宿主 5.4 构建输出>`）；
  正式快照仍为 5.3.0 时本版不能 submit，须宿主 5.4.0 同批部署。真机须核对 Ask 命令弹窗、
  Minerva 窗格在 Aurora 线程创建、`vulcan.app.show/hide` 中继与热重载后前端重新登记。
## 1.21.1 验证

- `SceneRegistrationContractTests`：跨 owner 场景声明拒绝；非活动场景页面在登记当拍隐藏；同 id
  热重载保留停靠节点、分栏和隐藏；重启与场景重置都回到用户保存的初始布局。
- `PageInvalidateContractTests` / `ModulePageLoaderContractTests`：刷新不再先撤整个 owner，移除的 id
  才撤销，避免重复页面与历史布局同时失真。
- `LabelModeGateTests` 与 `ShellChromeContractTests`：右栏调整开关和 Ctrl 标签覆盖恢复所有窗格。
- 真机核对：Diana PrintWindow 捕获 1279×836，SHA-256 `7686E4CB1E962E2DFC88B434DBD9FCAF1AA14C98E00D8A4E628C353B76D38B35`；
  Janus 为项目操作左上、控制台左下、项目总览右上、分支图谱右下。

## 1.21.0 验证

完整 Contracts **371/371 通过**。新增 StatefulButtonContractTests 覆盖：按钮执行中可见不定进度条、
重复点击只执行一次、错误恢复、行容器复用不串状态、同步阻塞业务不阻塞 UI、运行态等到数据刷新完成；
450/1100 两种窗口宽度下，两张 500 行规则表都获得有限视口且可滚到末尾。
真机热重载和只读截图核验随 submit 执行；不执行真实项目提交/推送/归档来测试按钮。

## 1.20.4 表格验收

- `TableUpdateContractTests`：万行虚拟化和滚动、相同快照零通知、未改行/列对象复用、增量增删改、
  选中保留、无效批次不落地；真实命令总线路径验证 revision、连续刷新合并与失败保留内容。
- `TableHeaderContractTests`：Padding 表头隐藏，列头仍保留动作模板。
- `TableProportionContractTests` / `TableWidthContractTests`：不同视口宽度不溢出，末列承担剩余宽度。
- 真机观察必须使用 Diana；初次捕获主窗口因最小化被拒绝。
- 2026-09-13 实测：完整 Contracts **367/367 通过**；质量门禁通过；submit 已成功热重载 1.20.4。
  宿主 PID 未变化，Aurora commandCount=63，attached=true，instanceId 已变化。
- 新版真机视觉通过 Diana `PrintWindow` 后台捕获并实际读取 PNG：**1279×836**，
  SHA-256 `53F1F00D0D3DD8457E68537E85A3FA854EFE0CDB840B41C05DCC3B2A1455ABE7`。
  Janus 项目表头与上方面板两端对齐、圆角一致；表格底部显示“已更新 · 47 条 · 更新时间”。
  未进行其它主题的真机视觉核验。业务模块增量接口接入未纳入本轮，不将组件测试等同于业务扫描提速实测。


## 1.20.3 Release Checks

- `VERIFY-TOPBAR-RESIDUE`(REQ-UI-103):顶栏残留清干净且行为不变。页签模板只剩一块透明 Border、页签容器仍在
  (否则内容区空白);`DelayedDragGesture`、`DockingDragSession.Target` / `RequiresHold`、`FindDescendants<T>`、
  令牌 `Aurora.Space.TabStrip` 全部删除;停靠层七个 partial 收成三个,`ISceneDocking` 搬进 `DockingContracts.cs`。
  判据是 1.20.2 的全部用例**原样通过**(本条不新增行为用例):`PanesHaveNoTabRow`、
  `InvisibleCaptionStaysZeroAndNoPaneDrawsATitle`、`LabelModeCoversEveryPaneWithItsPageTitle`、
  `DockingContractTests` 全部、`SceneContractTests` 全部。删掉的 5 条 `DelayedDrag*` 用例随被测对象一起走。
- `VERIFY-RAIL-CANVAS`(REQ-UI-104):右栏 `BorderThickness` 为 0、底色等于 `Aurora.Brush.Canvas`,
  浅色深色各验一遍;深色下断言它**不等于** `SurfaceAlt`——那正是这次报上来的形态。
  判据是 `ShellChromeContractTests.RightRailSharesTheDockingCanvasAndDrawsNoSeam`。
- `VERIFY-LABEL-RADIUS`(REQ-UI-105):每一块 `PageLabelCover` 的圆角等于 `Aurora.Radius.Inner`。
  判据是 `ShellChromeContractTests.LabelModeCoverKeepsTheCardCorners`。
- `VERIFY-RAIL-SEARCH`(REQ-UI-106):`NavOverlay` / `NavResults` 不存在;输入筛场景与页面两段、
  筛空时两段都空且出「没有匹配的场景」、清空恢复原样。
  判据是 `ShellChromeContractTests.RailSearchFiltersScenesAndPagesWithoutAnOverlay`。
- `VERIFY-DRAG-SURFACE`(REQ-UI-107):右栏里的列表面板判为拖动面,按钮、搜索框、常用页面胶囊判为控件。
  判据是 `ShellChromeContractTests.RailBlankSpaceIsAWindowDragSurface`。
  停靠区画布那一半靠「命中的是 DockingManager 自己」这条判据,无法在无人值守下发真实命中,留真机验。
- 2026-09-12 实测:Release 构建 0 警告 0 错误;`Contracts` 363 全绿(359 条旧用例 + 4 条新用例;
  1.20.2 的 364 条里有 5 条随 `DelayedDragGesture` 删除)。
- **真机验收:待人手验。** 右栏与页底同色、无竖线(深色尤其看);按住 Ctrl 时页面四角仍是圆角;
  右栏任意空白处都能拖窗口、双击最大化,胶囊仍只能拖不能点;右栏搜索框输入后场景与常用页面同时筛,
  回车打开第一个候选;`Ctrl+Alt+K` 聚焦搜索框而不是弹浮层。

## 1.20.2 Release Checks

- `VERIFY-ONE-PAGE`(REQ-UI-100 改写):任何时刻一格一页,没有等位。程序放进目标页那一格顶掉原来那一页;用户拖进一格
  (先浮出、再从浮窗落进那一格,等价于「放进这一格」停靠点)留拖进来的页;占位页随后被隐藏,那一格空着,被顶掉的页不回来。
  判据是 `DockingContractTests.EveryPaneKeepsOnlyTheLastPageIn`、`PageRegisteredIntoAnOccupiedPaneStaysHiddenWithoutWaiting`、
  `RegisteredCenterPageNeverTakesTheCenterFromAPageAlreadyThere`、`SideToolCanBeDraggedIntoTheMainDocumentPaneAndBackOut`。
- `VERIFY-FOLLOWER-PLACE`(REQ-UI-100 第 4 条):跟随页的目标藏着时按目标上次露面的方位落位,不按目标默认方位;
  跟随页先于目标登记时留在兜底方位。判据是 `MissingOrCorruptSnapshotFallsBackToPlacementsWithoutDeletingIt`(三组负载)、
  `FollowerRegisteredBeforeItsTargetKeepsItsFallbackPlace`。**本轮修掉的缺陷**:上一轮半成品只认露着的目标,
  台账恢复时跟随页落到目标的默认方位(左侧),与台账记的右侧不符。
- `VERIFY-SCENE-FRESH`(REQ-UI-100 第 5 条):当前场景里新登记的本场景页,外来页被场景藏掉后位置空着就露面。
  判据是 `SceneContractTests.PagesRegisteredWhileInAnotherSceneStayOutOfSight`(`ISceneDocking.ShowIfSeatFree`)。
- `VERIFY-MCP-TOOL`(REQ-UI-102):布局里一个 `LayoutDocument` 都没有;命令集可以停到右侧。判据是
  `NoPageHasADocumentIdentity`、`CommandCatalogIsAnOrdinaryToolPage`,以及按工具页身份改写的
  `RestoreSkipsRemovedPageAndAddsTheCurrentCommandCatalog`、`FloatingCenterPageRoundTripsWithoutInvalidatingMainLayout`、
  `JsonSnapshotPreservesNestedSplitsTabsSelectionAndMultipleFloatingWindows`。
- `VERIFY-NO-TAB-ROW`(REQ-UI-101):窗格没有页签行、没有页头动作位、没有浮窗最大化按钮;专注态控制组仍在右栏;
  拖动只从 Ctrl 标签态的标签起手。判据是 `ShellChromeContractTests.PanesHaveNoTabRow`、`InvisibleCaptionStaysZeroAndNoPaneDrawsATitle`、
  `NoWindowDrawsAFloatingMaximizeButton`、`FocusedPageKeepsTheWindowChromeInTheRightRail`、`PageDragStartsOnlyFromTheLabelInLabelMode`、
  `EveryPaneRoutesPointerInputToThePageDragCoordinator`、`PageDragGestureTests.TopBarGesturesAreGoneFromTheDragCoordinator`。
- `VERIFY-LABEL-NOW`(REQ-UI-097,DEC-034 决策 4):单独按下 Ctrl 当拍进,没有 0.3 秒延迟。判据是 `LabelModeGateTests`。
- 删除的旧用例 7 条(断言对象随顶栏删除):`ChromeBarReservesRoomSoTabsNeverHideUnderTheButtons`、`CentralTabsUseOneVisualSelectionSource`、
  `FocusedToolContentStartsBelowItsChromeRow`、`MainChromeHeaderStartsAWindowGestureEvenThoughThePaneIsASelector`、
  `ToolPaneHeaderKeepsOnlyTheFocusRestoreAction`、`MaximizedPageMovesTheSingleMainChromeIntoTheFocusedHeader`、
  `FloatingWindowChromeContractTests.TabsAndPaneButtonsDeclareTheirChromeExemption`。
- 上一轮交接时的「冒烟失败」:`Contracts` 371 条里 27 条红——26 条是旧用例仍在断言已删除的行为(等位、命令集文档身份、
  延迟标签组目标、顶栏页签与页头),1 组(三条)是上面 `VERIFY-FOLLOWER-PLACE` 的真缺陷。
- 2026-09-11 实测:Release 构建 0 警告 0 错误;`Contracts` 364 / `Smoke` 1 / `ModuleSmoke` 4 全绿;`Test-QualityGate.ps1` 通过。
  候选包 `z-Publish/HistoryAurora-v1.20.2` 已生成。
- **部署**(2026-09-11,宿主 5.3.0,`vulcan.module.install`):装上即附着(`attached=true`,无附着失败,63 条指令)。
  截图已验:窗格没有页签行、没有顶栏;窗口控制组在右栏顶部;左侧两格各一页;右栏「常用页面」胶囊在。
  部署后发现 `aurora.scene.open` 的说明仍写「顶掉顶栏」,改文字后重打包、`Contracts` 364 全绿、再装一次。
  **未确认**:`aurora.ui.windows` 报「组件测试」浮动,主窗口截图中央却是它的内容,且本机只截到 Mercury 扩展坞一个副窗口;
  可能是 1.20.1 存下的布局遗留。第二次热装后截图(当时在 Juno 场景)三格各一页,「组件测试」回到常用页面胶囊里(隐藏),
  浮动态没有复现;成因未查明。
- **真机验收:待人手验。** 拖入一格后该格只剩拖入的页、不再在几页之间循环;命令集能拖到侧边;窗格没有页签行;
  按下 Ctrl 立刻出标签;「常用页面」胶囊点一下不换页、按住能拖出。

## 1.20.1 Release Checks

- `VERIFY-PANE-SINGLE`(REQ-UI-100):每一格只留一页。程序并标签组顶掉目标页;用户把**非中央登记**的工具页拖进主文档区、
  再拖进底边格,两段都留拖进来的页(模型直改、不经抑制,等价于 AvalonDock 落停靠)。
  判据是 `DockingContractTests.EveryPaneKeepsOnlyTheLastPageIn`。
- `VERIFY-SEAT-WAIT`(REQ-UI-100 第 4 条):登记不抢位、藏着等位;占位页被明确隐藏时等位页回来,只等一次。
  判据是 `PageRegisteredIntoAnOccupiedPaneTakesTheSeatWhenItFreesUp`、`SceneContractTests.PagesRegisteredWhileInAnotherSceneStayOutOfSight`、
  `RegisteredCenterPageNeverTakesTheTopBarFromAPageAlreadyThere`(末段按等位改写:命令集隐藏后 module.page 回到顶栏)。
- `VERIFY-LABEL-HOLD`(REQ-UI-097 第 3 条):单独按住满 0.3 秒进一次、碰别的键作废、Aurora 不在眼前不进。
  判据是 `LabelModeHoldGateTests` 四条;标签视觉仍由 `LabelModeCoversEveryPaneWithItsPageTitle` 守(单个 TextBlock 写页名)。
- `VERIFY-NATIVE-DRAG-OFF`(REQ-UI-098 第 2 条):文档页签、工具页签按下后 AvalonDock 的原生拖动被卸掉。
  判据是 `ShellChromeContractTests.NativeTabDragIsDisarmedOnEveryPress`(先手工上膛再按,处理器没跑就红)。
- 按新规矩改写的旧用例 12 条:`MissingOrCorruptSnapshotFallsBackToPlacementsWithoutDeletingIt`(三组负载)、
  `SnapshotPlacementWaitsForALateRegisteredPage`、`JsonSnapshotPreservesNestedSplitsTabsSelectionAndMultipleFloatingWindows`、
  `RestoreSkipsRemovedPageAndAddsTheCurrentCommandCatalog`、`LateRegisteredDefaultModuleWindowJoinsExistingRightPane`、
  `HiddenPageKeepsItsLastTabGroupWithoutASettingsService`、`UnsupportedOrDuplicateSnapshotFallsBackToDefaultLayout`、
  `RestoredSeparatePanesOnTheSameSideRemainSeparate`,以及 `SceneContractTests` 三条(Janus 场景「图」顶掉同格的控制台)。
  改写只动「同一格两页并存」的断言,位置、恢复、延迟登记的原意照验。
- 2026-09-11 实测:Release 构建 0 警告 0 错误;`Contracts` 391 / `Smoke` 1 / `ModuleSmoke` 4 全绿;`Test-QualityGate.ps1` 通过。
- **真机验收:待人手验。** 焦点在别的程序、鼠标停在 Aurora 上按住 Ctrl 能进标签态;Ctrl+C / Ctrl+点选不闪;
  标签拖出落到停靠点嵌入、落空隐藏;顶栏页签按住拖出不再产生悬浮窗;非中央页拖进主窗口后每格只一页;
  1.20.0 存下的多页标签组启动后每格只一页。

## 1.20.0 Release Checks

- `VERIFY-SCENE-NOPAGES`(REQ-UI-094):场景只是一份布局。首访露出本模块的页与常驻页、场景各记各的显隐、
  重置回到首访、另存场景即命名布局、`scene.open` 不切场景、1.19.0 设置照读。判据是 `SceneContractTests` 全部十条。
- `VERIFY-MCP-UNANCHORED`(REQ-UI-095):命令集可藏、可浮,藏着越过布局去抖也不被放回,停靠只认中央区。
  判据是 `DockingContractTests.CommandCatalogIsNoLongerTheCenterAnchor`。
- `VERIFY-CENTER-SINGLE`(REQ-UI-096):顶栏只留最后显示/停靠进来的那一页;被顶掉的页原容器没了也能回到中央区。
  判据是 `DockingContractTests.CenterHoldsOnlyTheLastPageShown`、`StockDocumentPaneCreatedByAvalonDockIsUpgraded`,
  `ShellChromeContractTests.CenterTabRowKeepsOnlyTheLastPageShown`(断言落在控件上)。
  依赖多页顶栏的 11 条旧用例按新规矩改写(先 `Show` 再验位置与身份),拆分中央区改用「侧边页丢到中央区边上」造。
- `VERIFY-LABEL-MODE`(REQ-UI-097):标签态盖住每格窗格且写着页名;页签按下不判 Handled,平时不起拖动会话,
  标签态才起。判据是 `LabelModeCoversEveryPaneWithItsPageTitle`、`CenterTabPressIsNeverHandledAndDragsOnlyInLabelMode`。
  用例改发隧道的 `PreviewMouseDown`:`PreviewMouseLeftButtonDown` 是直达事件,手工发只到页签自己,会白绿。
- `VERIFY-PARK`(REQ-UI-098):收起的浮出页回到停靠位、不顶掉顶栏。判据是 `ParkedPageComesBackDockedAndLeavesTheCenterAlone`。
- `VERIFY-RIGHT-RAIL`(REQ-UI-099):窗口控制组在右栏顶部、右缘贴窗体;工具页头只剩「恢复页面」、模板里没有弹出菜单;
  右栏胶囊只列没露面的页。判据是 `WindowChromeBarSitsAtTheTopOfTheRightRail`、
  `ToolPaneHeaderKeepsOnlyTheFocusRestoreAction`、`RightRailListsHiddenPagesAsCapsules`。
- **部署**(2026-09-11,宿主 5.3.0,`vulcan.module.install`):
  1. 第一次装失败回滚:`module.manifest.json` 仍写 1.19.0,宿主日志报「manifest 身份与程序集声明不一致」。
     manifest 版本是手改的,不跟 `AuroraVersion.props` 走——已改,门禁不查这一项。
  2. 第二次装上(`attached=true`,域指令 67 → 65,正好少了 `scene.add / remove`)。截图发现首屏顶栏空了:
     1.19.0 位置台账里「上次选中」的模块页登记时抢走顶栏,又被场景藏起。已改为新登记的中央页一律不抢顶栏
     (REQ-UI-096 第 3 条),补门禁 `RegisteredCenterPageNeverTakesTheTopBarFromAPageAlreadyThere`,第三次装上。
  3. 已验(截图):窗口控制组在右栏顶部、窗体右上角;场景列表与「常用页面」胶囊在右栏;同名页胶囊附模块名
     (「命令集 · Aurora」「命令集 · Mercury」);顶栏只一页;工具页头没有 ▼ ✕。
- **真机验收:部分。** 待人手验:单独按住 Ctrl 0.3 秒进标签态、Ctrl+C 不闪;
  标签/页签/胶囊三条来路拖出后蓝色停靠点出现、落到停靠点嵌入、落空隐藏并在胶囊里出现;
  拖入顶栏后原来那一页被顶掉;右栏按钮与菜单、错误徽章、专注态控制组挪位;1.19.0 多页布局升级后只留一页。
- 2026-09-11 实测:Release 构建 0 警告 0 错误;`Contracts` 384 / `Smoke` 1 / `ModuleSmoke` 4 全绿;
  `Test-QualityGate.ps1` 通过。Debug 构建本轮未跑。
- **已知问题(本轮收尾不修,用户 2026-09-11 拍板先结束这一轮)**:
  1. 用户真机试用后反馈**仍有多处 bug 与未完成部分**,本轮未逐条记录,下一轮开工时先收集。
  2. 未确认的异常:部署后截图停在 Mercury 场景,中央区却是 Juno 的页、控制台停到了左侧。
     当时用户正在来回切场景,无法区分是手动操作还是场景恢复出错;可能与 1.19.0 存下的各场景命名布局
     (含多页顶栏、旧页面集时代的显隐)在新模型下的恢复有关,待复现。
  3. 未完成:顶栏(主文档区页签行)本身尚未删除;REQ-UI-097 / 098 的按键时序与三条拖出来路未经真机逐项验收。

## 1.19.0 Release Checks

- `VERIFY-SCENE-DERIVED`(REQ-UI-084):每个模块自动得到一个场景,切过去只剩它的页与常驻页;
  「全部」切回来每页都在。判据是 `SceneContractTests.EachModuleGetsASceneAndSwitchingKeepsOnlyItsPages`,
  夹具照真实两极端搭(Janus 三页含并入控制台的标签组、Minerva 一页)。
- `VERIFY-SCENE-LAYOUT`(REQ-UI-085):场景布局存为按场景 id 命名的命名布局。
  判据是 `SceneLayoutsAreNamedLayoutsKeyedByModuleName`。
- `VERIFY-SCENE-LATE`(REQ-UI-085):在别的场景里新登记的页不得露面,本场景的新页照常露面。
  判据是 `PagesRegisteredWhileInAnotherSceneStayOutOfSight`。
- `VERIFY-SCENE-EDIT`(REQ-UI-085):增删只改指定场景、重置丢掉改动、打开场景外的页会切到含它的场景、
  当前场景与频次跨重启保留。判据是 `AddAndRemoveChangeOnlyThatScene`、`ResetDropsTheUsersChanges`、
  `OpeningAPageOutsideTheSceneSwitchesToTheSceneThatHasIt`、`ActiveSceneAndUsageSurviveARestart`。
- `VERIFY-SCENE-VIEWS`(REQ-UI-086):切场景不重建页面视图,「只做显隐」与「恢复快照」两条路都验。
  判据是 `SwitchingScenesKeepsThePageViews`——比的是内容对象引用,不是页数。
- `VERIFY-SCENE-BROKEN`(REQ-UI-087):另存场景引用的页没了照样能切,缺页有名有姓。
  判据是 `SavedSceneReportsPagesThatAreNoLongerRegistered`。
- `VERIFY-MENU-GROUPS`(REQ-UI-085):顶层菜单从 5 组变 6 组,「场景(_S)」夹在视图与工具之间。
  判据是 `ShellChromeContractTests.FoldedMenuKeepsAllGroupsAndConsumerToolActions`(本版改了期望值)。
- **真机验收**(2026-09-10,宿主 5.3.0,经 `vulcan.module.install` 热装两次,均 `attached=true`):
  1. 已验:首屏与升级前一致——当前场景「全部」,每页都在(截图)。
  2. 已验:左栏列出「全部 / Aurora / Janus / Juno / Mercury / Minerva」,点击切换有效;
     Juno 场景只剩命令集(常驻)、账号导入、Juno 与控制台,Aurora 场景只剩自持页与常驻页(截图)。
     `aurora.scene.list` 在活宿主上返回六个场景、零断链,页面集与模块一一对应。
  3. 已验:热重载后当前场景保留(重载前后都停在用户最后切到的场景)。
  4. **真机先发现、已修**:导航器热键第一次热装后**没有注册**——注册挂在 `aurora.host.ready` 上,
     而单独热重载 Aurora 时宿主不重发就绪通知。改为挂在每轮模块发现之后、成功一次即止;
     第二次热装后 `mercury.hotkey.list` 出现 `aurora.nav → Ctrl+Alt+K → aurora.nav.open`。
  5. 已验(注册表层面):`aurora.scene.*` 八条的 `HiddenReason` 为空,`aurora.nav.open` 有 `HiddenReason`。
     **未在 MCP 工具表里亲眼看到**——验收会话的工具表是开场时拉的,要新会话才刷新。
  6. **仍待人手验**:按 `Ctrl+Alt+K` 呼出浮层与 Enter / Shift+Enter / Esc;专注态(F11)左栏隐藏;
     左栏空白处拖动窗口、双击最大化;在 Minerva 场景里热重载 Janus,它的页不挤进来。
- 2026-09-10 实测:Release 构建 0 警告 0 错误;`Contracts` 381 / `Smoke` 1 / `ModuleSmoke` 4 全绿;
  `Test-QualityGate.ps1` 通过。Debug 构建本轮未跑。

## 1.18.4 Release Checks

- `VERIFY-CENTER-INDEX`(REQ-UI-083):中央文档窗格必须认得工具页的下标。判据是
  `ShellChromeContractTests.StockDocumentPaneCannotSelectAnAnchorableButAuroraCenterPaneCan`
  ——它直接对比原装 `LayoutDocumentPane` 与 `CenterDocumentPane`,回滚子类会立刻红。
- `VERIFY-CENTER-TOOL-TAB`(REQ-UI-083):中央区工具身份的页能被选中,两条路都要通
  (`IsSelected` 与窗格的 `SelectedContentIndex`)。判据是 `CenterToolTabPressSelectsThatPage`。
- `VERIFY-NO-TAB-INTERCEPT`(REQ-UI-083):页签左键不得被 Aurora 拦。判据是
  `NeitherCenterTabKindIsInterceptedOnPress`——这条守的是三次失败的教训,不是一条新契约。
- `VERIFY-SHOW-SWITCHES`(REQ-UI-083):`aurora.ui.show` 对中央工具页必须真的换页,
  不能只是报成功。判据是 `DockingContractTests.ShowSelectsACenterToolPage`。
  **这条是真机先发现、门禁后补的**:1.18.2 的模型层已经修对,真机却仍然点不动——
  因为屏幕上那个窗格是 AvalonDock 在此前的拖动里自己建的原装货。
- `VERIFY-CONTROL-FOLLOWS`(REQ-UI-083):换页要换在**屏幕上**,不能只改模型。判据是
  `ShellChromeContractTests.ShowMovesThePaneControlNotJustTheModel`。
  这条同样是真机先发现的:模型层修对之后,布局快照里 `selectedContentId` 已经是对的,
  而窗格控件仍停在原处。
- `VERIFY-NO-MAIN-PAGE`(REQ-UI-083):注册协议给不出主页面——文档身份只属于命令集。
  判据是 `DockingContractTests.RegistrationNeverProducesASecondMainDocument`。
  这条守的是一条一直成立、却没人守着的约束。
- `VERIFY-PANE-UPGRADE`(REQ-UI-083):AvalonDock 建出来的中央文档窗格要在布局差分里
  被换掉,孩子、尺寸与选中页原样保留。判据是
  `DockingContractTests.StockDocumentPaneCreatedByAvalonDockIsUpgraded`。
- **本轮走过并撤回的两条路线,留档**:
  1. 把中央区拆成「命令集主文档区 + 中央工具页组」。结构上根治、367 条契约全绿、
     真机也跑起来了,但它把一排页签变成上下两块——中央区就该是一排页签,
     模块页叠在命令集上。撤回。(1.18.0)
  2. 用类处理器只接管「工具身份」那一半页签的左键,换页自己写下标。
     真机当场坏:`SelectedContentIndex` 的 setter 会回头去写 `IsSelected`,
     后者又按坏掉的 `IndexOf` 回写 -1,净效果是中央区点不动了。撤回。(1.18.1)
  两次都指向同一件事:**病根是那个 -1,不在手势层**。1.18.0 / 1.18.1 是未落地的
  中间形态,版本号不复用。
- **仍需真鼠标收的一条**:中央工具页签的拖出。合成事件驱不动系统的窗口移动循环,
  跑 `AURORA_MOUSE_SMOKE=1` 的浮窗冒烟,或在真机上拖一次项目总览。
- 2026-09-07 实测:Debug 与 Release 构建均 0 警告 0 错误;`Contracts` 372 / `Smoke` 1 /
  `ModuleSmoke` 4 全绿;`Test-QualityGate.ps1` 通过;真机装载后中央区页签点得动。

## 1.17.6 Release Checks

- `VERIFY-TAB-SWITCH`(REQ-UI-082):点中央页签要换页。判据是
  `ShellChromeContractTests.TabPressSwitchesWhereItCanAndNeverBlanksThePane`。
- **本轮的教训,留档**:「让 Aurora 接管页签左键」这条路走了两版、三种写法,全错。
  隧道 Handled + 写模型选中 → 窗格选中下标被反向写成 -1,中央区点一下就空;
  类处理器 Handled → AvalonDock 断了,容器 TabItem 也一起断,页更换不了;
  自己写控件层选中 → 文档区里混着隐藏页时错位一格(点 commanddetail 选中 components)。
  交回 AvalonDock 才对。**Aurora 该管的只有拖,而拖的病根是捕获没放掉,不是按下被谁收走。**
- **挂账,本轮不修**:
  1. 中央区分栏(把中央页丢到中央区侧边)会留下空白窗格;
  2. 中央页全是 `LayoutAnchorable` 而装在 `LayoutDocumentPane` 里,模型层选中对它们
     永远失效(`LayoutDocumentPane.IndexOf` 只认 `LayoutDocument`,纯模型探针实测)。
     快照里的「上次选中哪一页」因此也存不下来(推论,一条探针可坐实)。
  两条同源,要修得先定 A 路线的落点。
- **合成点击判不准的那一半**:工具身份的中央页,合成事件点下去会落到相邻的隐藏页上,
  真机行为要靠真鼠标才判得准(`AURORA_MOUSE_SMOKE=1`)。因此用例只断言文档身份那一半。
- 2026-09-06 实测:Release 构建 0 警告 0 错误;`Contracts` 365 / `Smoke` 1 / `ModuleSmoke` 4
  全绿;`Test-QualityGate.ps1` 通过。

## 1.17.5 Release Checks

- `VERIFY-TAB-CLASS-HANDLER`(REQ-UI-080):两种页签的左键都必须在冒泡路由上被断掉。判据是
  `ShellChromeContractTests.AvalonDockOwnTabPressImplementationIsCutOffOnBothTabKinds`,
  停掉类处理器后会失败,已实测确认。
  **教训留档**:1.17.2 只在隧道阶段判定 Handled,合成用例(发 `Mouse.PreviewMouseDown`)
  绿着,真机照旧走 AvalonDock 那条路——所以这条用例发的是冒泡的
  `UIElement.MouseLeftButtonDown`,专门盯类处理器这一层。
- `VERIFY-QUIET-DRAG`(REQ-UI-081):探针与六条逐次回声已删,失败路径的 Error/Warn 保留。
  守探针只读的那条用例随之退役,契约用例总数 366 → 365 →(新增一条)366。
- **本轮挂账,不修**:中央区分栏(把中央页丢到中央区侧边)会留下一个空白窗格。
  根因已查清并留档:`LayoutDocumentPane.IndexOf` 只认 `LayoutDocument`,
  传 `LayoutAnchorable` 返回 -1,于是模型层选中永远写不进去;而 Aurora 的中央页全是
  anchorable。用户判定该路径「反正也用不到」,本轮不动。
- **1.17.3 / 1.17.4 已撤销**:见 `656b3f5`。那条路线(不分栏 + 自动并回)连带引出
  页签劫持与模块页被隐藏,回到 1.17.2 分叉点重来。
- 2026-09-06 实测:Release 构建 0 警告 0 错误;`Contracts` 366 / `Smoke` 1 / `ModuleSmoke` 4
  全绿;`Test-QualityGate.ps1` 通过。
- **2026-09-06 22:03 真机已部署**:`vulcan.module.install` 进程内热重载进活宿主 5.3.0,
  回执 `version 1.17.5`、`attached=true`、`attachFailures` 为空、56 条指令。重载后前端
  日志 50 条里 Error/Fatal 为 0,`aurora.ui.windows` 13 页齐全。
  **真机复验待办**:点中央页签与点工具页签都应当只是换页,不再拖起窗口;拖动时控制台
  不应再出现「停靠探针」与「拖动会话」的逐次回声。两条需人工点/拖一次确认。

## 1.17.2 Release Checks

- `VERIFY-TAB-ONE-OWNER`(REQ-UI-077):中央页签的左键按下必须在隧道阶段判定 Handled,
  且选中仍然生效。判据是 `ShellChromeContractTests.CenterPageTabPressHasOneOwnerAndStillSelectsThePage`
  全绿。注意用例必须发**隧道的** `Mouse.PreviewMouseDown`——`PreviewMouseLeftButtonDown`
  是 Direct 事件,由 WPF 在路由每一站上就地提升,直接发它只打到页签自己身上,
  挂在 `DockingManager` 上的处理器根本不会被调用(本轮在这里绕过一次)。
- `VERIFY-PANE-HAS-CONTENT`(REQ-UI-077):非空文档区必须有选中内容。判据是
  `DockingContractTests.DocumentPaneWithChildrenAlwaysKeepsASelectedContent` 全绿。
  两条用例在回滚源码修改后都会失败,已实测确认。
- 2026-09-06 实测:Release 构建 0 警告 0 错误;`Contracts` 366 / `Smoke` 1 / `ModuleSmoke` 4
  全绿;`Test-QualityGate.ps1` 通过。
- **2026-09-06 20:45 真机已部署**:`vulcan.module.install` 进程内热重载进活宿主 5.3.0,
  回执 `version 1.17.2`、`attached=true`、`attachFailures` 为空、56 条指令。重载后前端
  日志 50 条里 Error/Fatal 为 0,`aurora.ui.windows` 13 页齐全。
- **真机复验待办**:本轮的根因只有真鼠标拖得出来。合成事件能压住「Handled + 选中」
  和「窗格必有选中内容」两条,但「拖出去 → 浮窗宿主如期建立 → 不再有
  PointToScreen 异常」这一条要人工拖一次中央页签确认,或开
  `AURORA_MOUSE_SMOKE=1` 跑鼠标冒烟。未确认前不得记为已通过。

## 1.17.1 Release Checks

- `VERIFY-SPLIT-CENTER`(REQ-UI-076):中央区被拆成两个
  `LayoutDocumentPane` 之后——真机路径是把中央页拖出去、再丢回中央区——布局差分、
  换边停靠与存盘都不得塌。判据是 `DockingContractTests.SplitCenterKeepsDiffsDockingAndSavingAlive`
  全绿;把源码改动回滚后该用例会以真机同款堆栈失败(`DetectSide` → `ComputeAllStates`
  → `RebaseSoon`,`Sequence contains more than one element`),已实测确认。
  改点:取中央主文档区的三处收敛到 `TryFindMainDocumentPane`(第一个不在浮窗内的文档区)——
  `DockingHost.State.cs` 的 `DetectSide`、`DockingHost.Placement.cs` 的 `EnsureCenterColumn`、
  `DockingHost.Layout.cs` 的 `LayoutHasMainDocumentPane`;`Single/SingleOrDefault` 从停靠层清零。
- `VERIFY-TEST-TRIM`:本轮精简后 `Contracts` 用例总数仍是 364,净删约 210 行——
  删 `DockSideKeepsLegacyTabValue`(只钉枚举序号,而布局 JSON 走 `JsonStringEnumConverter`
  存的是名字、指令走 `SideText()` 也是名字,它守不到任何东西);
  `InvalidLegacyXmlFallsBackToTheCurrentDefaultLayout` 并入
  `MissingOrCorruptSnapshotFallsBackToPlacementsWithoutDeletingIt` 的第三条 InlineData
  (壳里已无 XML 反序列化路径,它就是又一份非 JSON 负载);
  `DockingOverlayBrushesKeepButtonsVisibleAndPreviewFillTransparent` 与
  `DockingThemeResourceDictionaryIsPrewarmedBeforeDrag` 合成
  `DockingOverlayBrushesAreDrawableInTheThemeSourceAndFromTheManager`,省一次整壳启动;
  `PanelComponentContractTests` 的 8 条 `Validate_Rejects*` 收成一条 `[Theory]` 9 例。
- 2026-09-06 实测:Release 构建 0 警告 0 错误;`Contracts` 364 / `Smoke` 1 / `ModuleSmoke` 4
  全绿;`Test-QualityGate.ps1` 通过。
- **2026-09-06 20:08 真机已部署**:`vulcan.module.install` 进程内热重载进活宿主 5.3.0,
  回执 `version 1.17.1`、`attached=true`、`attachFailures` 为空、56 条指令。重载后前端
  日志 50 条里 Error/Fatal 为 0(只剩 Sub2API 未启动带来的既有 Warn),`aurora.ui.windows`
  13 页齐全。**中央区分栏的真机手感尚未复验**:该场景由契约用例压住,真机需人工拖一次。
- 版本号目前有三处副本:`AuroraVersion.props`、`project.manifest.json`、
  `b-Code-Studio/Module/module.manifest.json`。本轮 1.17.1 打包时第三处漏改过一次,
  打出来的候选包里 `module.manifest.json` 仍写 1.17.0——`Test-QualityGate.ps1` 只单源化了
  宿主版本,不查模块自己的版本。下一轮应把这三处收成单源或补一条门禁。

## 1.17.0 Release Checks

- `VERIFY-HOST-READY`（REQ-UI-075）：`HostReadyContractTests` 三条全绿——
  `ReadyHookIsRegisteredWhereTheHostCanFindIt`（来源是前端目录，否则宿主抄不走、
  通知永远发不出来）、`ReadyHookRunsAFullSweepAndReportsWhatItAsked`、
  `ReadyHookSurvivesAModuleThatAnswersNothing`。
- `VERIFY-OLD-HOST-COMPATIBLE`（REQ-UI-075）：本版**不抬** `MinimumHistoryVulcanVersion`。
  在宿主 5.1.2 上装载后，`aurora.host.ready` 只是注册表里多出的一条命令，
  没有任何路径会调用它，界面行为与 1.16.0 一致。
- 2026-09-04 实测：Release 构建 0 警告 0 错误；`Contracts` 364 / `Smoke` 1 / `ModuleSmoke` 4 全绿。
- **真机联调待办**：本条要与宿主 5.1.3 一起验。判据是冷启动后宿主日志出现
  「已通知 N 个模块：宿主装载完成」，界面这一侧出现一条 `aurora.host.ready` 的就绪回执，
  且回执里的模块数与 `vulcan.module.list` 中带 UI 的模块数一致；
  控制台不再出现启动期的 `未知指令: <别的模块域>.*`。宿主未部署 5.1.3 之前这条无法完成，
  不得记为已通过。

## 1.16.0 Release Checks

- `VERIFY-LAYER-DIRECTORY`（REQ-UI-074）：`LayerContractTests` 四条全绿——
  `EveryShellFileSitsUnderALayer`（`Shell/` 根部零散文件）、`NamespaceFollowsLayerDirectory`
  （目录、命名空间、层号三者对齐）、`DependenciesOnlyPointDownwards`、
  `BaseLayerKnowsNothingAboutComponents`。
- `VERIFY-RESOURCE-NAMES`（REQ-UI-074）：**这条只能人工核**。构建后读
  `HistoryAurora.dll` 的 `.g.resources`，资源名必须仍是
  `themes/*.baml`（9 条）、`console/consoleview.baml`、`shellwindow.baml`——
  层目录一旦漏出到 `Link` 里，资源会变成 `2-components/themes/…`，
  编译期零征兆，运行期建窗时才抛 `IOException`。
  2026-09-03 已核对：11 条与迁移前逐条一致。
- `VERIFY-LAYER-INVERSION-FIXED`（DEC-029）：基础层不得出现 `is ShellWindow` 与任何
  `Widgets.*` 类型；页面层不得引用 `FrontendCommandCatalog`。前两条由
  `BaseLayerKnowsNothingAboutComponents` 与 `DependenciesOnlyPointDownwards` 覆盖。
- `VERIFY-PACKAGE-DOCS`：候选 `z-Publish/HistoryAurora-v1.16.0/docs/` 含组件清单与模块 API 两份，
  已进 `SHA256SUMS`；1.15.0 轮转进 `z-Publish/history/`。
- 2026-09-03 实测：Release 构建 0 警告 0 错误；`Contracts` 361 / `Smoke` 1 / `ModuleSmoke` 4 全绿；
  `Test-QualityGate.ps1` 通过。
- **2026-09-03 21:13 真机已部署**：`vulcan.module.install` 进程内热重载进活宿主
  （PID 20324 不变，宿主 5.1.2），`attached=true`、`attachFailures` 为空、55 条指令。
  `aurora.app.window` 回「normal」，`diana.view.capture` 截图确认：暗色令牌、停靠分栏与页签、
  命令集表格、控制面板、控制台全部正常渲染——**这是 `VERIFY-RESOURCE-NAMES` 的真机判据**，
  层目录若漏进 `Link`，主题字典在建窗时就会抛 `IOException`。
  `diana.log.read minlevel=warn` 全窗口零 Error；仅有的四条 Warn 来自 HistoryJuno
  （Sub2API 未启动 + 它自己页面声明的旧问题），与本轮无关。

## 1.15.0 Release Checks

- `VERIFY-QUIET-FETCH`（REQ-UI-072）：`PageProtocolContractTests` 断言表格从总线取到行之后
  回显账为空；`CellActionContractTests` 断言只有单元格动作进账，表格取数不进。
- `VERIFY-WRITEBACK-SILENT`（REQ-UI-072）：`aurora.ui.panelset`、跟随通道回填与候选重取期间
  不得触发 `commitAction`；标记以 `try/finally` 恢复，回写处理器抛异常后面板仍能正常提交。
- `VERIFY-OPTIONBOX-CONTEXT`（REQ-UI-073）：`AuroraOptionBoxContractTests` 右键后
  `ContextMenu` 为 `null`、`OptionsMenu` 三项且选中项打勾；不要求先左键激活。
- `VERIFY-PACKAGE-DOCS`：候选包必须含 `docs/`，内容为 `b-Office/package/*.md`，并进 `SHA256SUMS`。
  少了这一步 `diana.docs.catalog` 里 aurora 那条的 `Documents` 会变成空数组——不报错，
  只是别的项目再也读不到组件清单与模块 API。1.15.0 一度漏打，已在打包脚本补回并复核通道。
- 2026-09-03 实测：Release 构建 0 警告 0 错误；`Contracts` 360 / `Smoke` 1 / `ModuleSmoke` 4 全绿；
  `Test-QualityGate.ps1` 通过；候选 `z-Publish/HistoryAurora-v1.15.0` 已经活宿主
  `vulcan.module.install` 热重载（55 条指令，attached）。

## 1.14.0 Release Checks

- `VERIFY-CONSOLE-LOG-SNAPSHOT`（REQ-UI-071）：`ConsoleLogSnapshotContractTests` 覆盖级别、
  来源、关键字、默认倒序、`after` 正序、多行消息、256 KiB 完整记录截断和实例身份；同时断言
  敏感字段、Bearer、API 密钥、凭据 URI 与分隔式凭据在缓冲入口脱敏，普通错误与堆栈保持完整。
- `aurora.log.snapshot` 必须为隐藏只读命令；不改变控制台筛选器，不读磁盘、不截图、不访问网络。
- 真实宿主验收由 Diana `diana.log.read` 写入唯一标记后核对 Info/Warn/Error、顺序与实例 ID。

## 1.13.0 Release Checks

- `VERIFY-TABLE-CELL-ACTION`（REQ-UI-068）：`CellActionContractTests` 覆盖按钮键盘语义、点击行与列键、
  行字段占位符、空值不触发，以及缺失动作的可见诊断和日志；`PageComponentsContractTests` 锁定
  `table.cellaction` 能力。
- `VERIFY-DIALOG-CHOICE`（REQ-UI-069）：`AuroraDialogContractTests` 覆盖 40 项滚动列表、默认选择、
  value 返回、确认/取消入口，以及畸形、空和缺字段 options 的拒绝；`PageComponentsContractTests`
  锁定 `dialog.choice` 能力。
- `VERIFY-DIALOG-CONTENT-ACTIONS`（REQ-UI-123）：`AuroraDialogContractTests` 覆盖带 options 的 content
  同时给出正文、候选表与一对按钮，以及不给 options 时仍是只有关闭键的只读预览；
  `PageComponentsContractTests` 锁定 `dialog.contentactions` 能力。
- `VERIFY-PANEL-ICON`（REQ-UI-070）：`PanelComponentContractTests` 覆盖 `refresh-cw` 图标加文字、
  未知图标与非按钮图标拒绝，以及无 icon 的旧按钮内容兼容；`PageComponentsContractTests` 锁定
  `panel.icon` 能力。

## 1.12.20 Release Checks

- `VERIFY-DOCK-LAYOUT-PERSISTENCE`（REQ-UI-067）：`DockingContractTests` 覆盖 JSON 不含运行时类型名、
  嵌套分栏与尺寸、页签顺序与选中项、多浮窗与离屏收敛、自动隐藏、隐藏页原标签组、延迟注册、聚焦期间保存、
  命名布局、损坏/未知版本/重复 ID 回退，以及写入失败仍保留旧文件并保存 `layout.placements`。
- `VERIFY-COLLECTIBLE-LAYOUT`：`ModuleLoadTests.LayoutSnapshotCodecRunsInsideACollectibleModuleContext` 将
  `HistoryAurora.dll` 与随包 `AvalonDock.dll` 装进可回收 ALC，JSON 编解码往返不得抛
  `NotSupportedException`。
- `VERIFY-DOCK-OVERLAY-NO-DIFF`：沿用 1.12.14 的覆盖层结构、命中尺寸、透明预览和只读探针门禁；
  `AuroraOverlay.xaml`、`DockingDragProbe`、`DockingOverlayResourceRepair`、`WindowDragDriver`、
  `DockingDragSession` 必须零差异。
- 2026-08-31 实测：Release 构建 0 警告 0 错误；`Contracts` 346 / `Smoke` 1 /
  `ModuleSmoke` 4 全绿；`Test-QualityGate.ps1` 通过；五个覆盖层/拖动保护文件与四份公开 API 基线零差异。
- `FloatingDockMouseSmoke` 已用真实鼠标完成整条链路：覆盖窗可见、蓝色像素 260、最终目标为
  `DocumentPaneDropTarget`，松手后浮窗数为 0，模块页与命令集回到同一页签窗格。
- 首次 `vulcan.dev.submit`（run `20260831-173438-HistoryAurora`）已提交候选并热重载到活宿主；
  新实例的只读窗口清单保留控制台下停靠 28%、模块/指令详情左停靠 30%、中央页与隐藏页状态。

## 1.12.19 Release Checks

- `VERIFY-SELECT-COMMITS-CONTENT`：`mode: select` 变更触发 `commitAction`；`sourcePicker` 的 `selectCommand` 展开 `{selection.*}` 且替换值加引号。`ActionDeclarationContractTests.ExpandPlaceholders_SubstitutesSelectionChannel`。

## 1.12.18 Release Checks

- `VERIFY-TABLE-HOST-WIDTH-ONLY`：列宽分摊不得订阅 ListView 的 `SizeChanged`，只跟表格外框宽度走；±400px 振荡在一轮内最多重启 3 次。`TableLayoutGuardContractTests`。

## 1.12.17 Release Checks

- `VERIFY-TABLE-WIDTH-LOOP`：滚动条显隐（约 17px）不得清零列宽分摊轮数；连续重启满 5 次后必须停。`TableLayoutGuardContractTests` 用 10000 次 ±17px 振荡不得重启。
- `VERIFY-SOURCE-COMMIT-AFTER-IDLE`：来源选择器在文件对话框关闭后须等到 `ApplicationIdle` 再改文字、提交、刷新本页表格。

## 1.12.16 Release Checks

- `VERIFY-PANEL-COMMIT-REFRESHES-PAGE`：来源选择器提交成功后必须触发本页 `aurora.ui.refreshdata`；宿主抄到注册表的界面指令不得用 `Dispatcher.Invoke` 同步等待 `ShowDialog`。

## 1.12.15 Release Checks

- `VERIFY-TABLE-FILE-SELECT-FREEZE`：表格列宽分摊不得因文件对话框关闭后的半像素抖动清零轮数；来源选择器失焦不得提交空路径；`aurora.ui.selectfile` 必须 `RestoreDirectory=true`；`selectdirectory` 必须自行恢复进程工作目录。

## 1.12.14 Release Checks

- `VERIFY-DOCK-OVERLAY-SEPARATED-HIT-ART`：`AuroraOverlay.xaml` 必须保留 AvalonDock 所需的全部模板部件和 23 个 `PART_*DropTarget`；外层命中样式固定为 40×40 DIP，内层方向图形裁剪为 28×28 DIP，视觉尺寸变化不得改变命中元素。
- `VERIFY-DOCK-OVERLAY-READONLY-PROBE`：`DockingDragProbe` 只能读取覆盖窗状态和像素，不得调用模板应用、遍历修复、设置 Width/Height/Transform 或调用 `DockingOverlayResourceRepair`。
- `VERIFY-DOCK-OVERLAY-TRANSPARENT-PREVIEW`：`PART_PreviewBox` 填充透明且不可命中，只保留 AvalonDock 的目标边框；蓝色方向控件由自有模板静态提供。

## 1.12.13 Release Checks

- `VERIFY-DOCK-OVERLAY-INDICATOR-VISUAL-SIZE`：停靠覆盖层四个 `PART_*DropTargets` 父 Grid 保留 AvalonDock 命中尺寸，但其方向按钮内容固定为 28×28 DIP，五向/九向星形 Path 固定为 122/204 DIP；不得因命中矩形 Stretch 成整窗蓝色控件。
- `VERIFY-DOCK-OVERLAY-INDICATOR-DIAGNOSTICS`：每次覆盖层采样记录四个停靠 Grid、方向 ContentControl、内部 Viewbox 与星形 Path 的实际尺寸，便于区分命中区域与视觉控件。

## 1.12.12 Release Checks

- `VERIFY-DOCK-OVERLAY-OUTLINE-ONLY`：`PART_PreviewBox` 填充画刷 Alpha 必须为 0，只保留边框与方向按钮；整页填充不得遮挡重叠浮窗。

## 1.12.11 Release Checks

- `VERIFY-DOCK-OVERLAY-ALL-HOSTS`：拖动采样遍历应用中所有 `OverlayWindow`，每个实例的 `PART_PreviewBox` 都必须安装视觉缩放；不能只修 `DockingManager._overlayWindow`。

## 1.12.10 Release Checks

- `VERIFY-DOCK-OVERLAY-VISUAL-TIMING`：视觉缩放安装不依赖 `PART_PreviewBox.Data` 已写入；`DragEnter` 后写入的 Geometry 必须继承元素级缩放。

## 1.12.9 Release Checks

- `VERIFY-DOCK-OVERLAY-VISUAL-CENTER`：`PART_PreviewBox` 使用元素自身中心的 `RenderTransformOrigin`，视觉缩放为 0.35；不得用几何坐标作为变换中心。

## 1.12.8 Release Checks

- `VERIFY-DOCK-OVERLAY-VISUAL-GUARD`：`PART_PreviewBox` 每次模板/移动采样都会重设 0.55 倍视觉缩放和 0.55 视觉不透明度；仅改变绘制，不改变 AvalonDock 投放命中矩形。

## 1.12.7 Release Checks

- `VERIFY-DOCK-OVERLAY-RESOURCE-TIMING`：覆盖层首次创建即执行画刷修复，即使尚未可见也不会保留 AvalonDock 默认 Alpha 96 预览画刷。

## 1.12.6 Release Checks

- `VERIFY-DOCK-OVERLAY-PREVIEW-SCALE`：`PART_PreviewBox` 仅对目标几何做 0.82 倍视觉缩放，命中矩形与停靠合并链路保持原值。

## 1.12.5 Release Checks

- `VERIFY-DOCK-OVERLAY-PREVIEW`：停靠预览框仍覆盖正确投放矩形，但填充画刷 Alpha 不超过 `0x40`，叠加时不会遮住其它页面内容。

## 1.12.4 Release Checks

- `VERIFY-DOCK-OVERLAY-SIZE`：浅色与深色主题的 `DockingButtonWidth/Height` 均保持在 20～32 DIP，当前值为 28，避免停靠控件遮挡工作区。

## 1.12.3 Release Checks

- `VERIFY-DOCK-OVERLAY-PREWARM`：`DockingManager.Theme` 使用的 `DictionaryTheme.ThemeResourceDictionary` 在主题赋值前已固化停靠覆盖层画刷；热重载后新建窗口不依赖拖动途中的资源补丁。

## 1.12.2 Release Checks

- `VERIFY-SWITCH-COMPACT`：带标签开关只生成一个控件单元格，描述文字由开关本体显示；Minerva 转换选项行使用 `even` 均布，且不再声明 `flex`。

## 1.12.0 Release Checks

- `VERIFY-DRAG-SESSION`：拖动会话状态转换、提前松手和旧会话迟到均有确定性断言；窗口拖动原语只允许由 `WindowDragDriver` 持有。
- `VERIFY-TAB-DRAG-FLOAT`：普通与专注页签均通过同一捕获/浮动/拖动路径；浮窗创建延迟不再静默丢失操作。

- `VERIFY-CLOSE-PERSISTENCE`（REQ-UI-063）：`DockingContractTests.SaveCurrentLayoutStillPersistsPlacementsWhenLayoutWriteFails`。
  布局 XML 写入失败时，窗口隐藏/停靠位置仍写入 `layout.placements`，重启后可恢复。
- `VERIFY-TAB-DRAG-FLOAT`（REQ-UI-064）：页签拖动越过阈值后由 Aurora 显式创建浮窗并继续系统拖动循环，
  使 AvalonDock 能创建蓝色停靠覆盖层，松手后可合并回目标页签组。覆盖层与真实鼠标移动循环仍需真机确认。

## 1.9.2 Release Checks

- `VERIFY-CATALOG-WIRED`（REQ-UI-057）：两条，缺一不可。
  `Data_FailsLoudlyWhenTheCatalogSessionIsMissing` 验「没接线会报错」；
  `TheCompositionRootActuallyWiresTheCatalogSession` 验「这台机器接上了」。
  **只有前一条的话，装配根再漏一次仍然全绿**——1.9.0 到 1.9.1 命令集空了两版，
  正是因为当时那一路返回的是空表而不是失败，没有任何一处会喊疼。
  后一条判的是源码里 `Catalog = _catalog,` 那一处赋值：装配根要跑起来得有
  真窗口、真停靠层，在门禁里立不起来，与 `ModulesPageContractTests` 判
  「热重载走 `aurora.ui.selectdirectory`」是同一种取法。

- `VERIFY-COMMAND-COLUMNS`（REQ-UI-058）：
  `Data_SplitsTheCommandNameIntoDomainClassAndMethod` 同时钉列声明与行数据两侧——
  完整指令名从列里去掉了，但 `name` 这个键必须还在，否则右键菜单、指令详情页
  与 `IShellCommandWorkbenchHost.CommandSelection` 会一起断，而断法是「点了没反应」。
  `Data_KeepsEverythingAfterTheDomainAndClassPrefix` 用一条四段名指令与一条无类指令
  钉「不是取第三段」：取第三段在三段名上完全正确，只有四段名会露出来。
  `AuroraTableContractTests.SetData_KeepsKeysThatWereNotDeclaredAsColumns`
  守组件那一侧的口径改动。

- `VERIFY-PANEL-OPTIONS-SOURCE`（REQ-UI-059）：`PanelOptionsSourceContractTests` 四条。
  判据是**框里的候选项**，不是「取数指令被调用过」——调用到了但结果没落到控件上，
  在界面上与根本没调用完全一样。
  `RefetchingKeepsTheChoiceThatSurvivesAndDropsTheOneThatDoesNot` 一条里验两面：
  留一个已经不在候选里的值，会让下一次取数带上一个这个域根本没有的类。
  写这条用例时自己先踩了一次：候选还没填上就给 `Selector` 赋 `SelectedItem`，
  值会被静默丢掉——那正是动态候选与静态候选真正的区别。

- `VERIFY-PANEL-ROWS`（REQ-UI-060）：`PanelComponentContractTests` 覆盖行模式、折行、标签单元格和开关本体布局。
  版面这一层只对外留一份快照 `AuroraPanelBoard.CellBounds`（声明行 → 视觉行 → 每格的 Rect），
  均布、可变、折行三条规则的断言全从它来。
  两条最容易写错的：`RowWrapsOnlyWhenItCannotFitAndStaysOneDeclaredRow` 必须**同时**
  断言「折成了两个视觉行」与「声明行仍然只有一行」——只断前者的话，
  折行顺手多画一条横分隔线也照样全绿；`WrappedLinesKeepTheParentRowMode`
  钉住折出来的行按母行模式分配余量，而不是退回缺省。
  测量宽度时要**把排版面自带的左右各 6 内缩算进去**：直接按窗口宽度写期望值会全部差 12，
  这里踩过一次。

- `VERIFY-PANEL-INSET`（REQ-UI-061）：
  `PanelAddsNoVerticalInsetBeyondTheSharedSurface` 判的是排版面与**每个控件**的
  上下 Margin 都为 0，不是「我改了一个常量」——1.9.2 之前厚出来的那 7～10px
  是三层叠加的结果，只盯其中一层会让另外两层悄悄长回去。

- `VERIFY-COLUMN-ORDER`（REQ-UI-062）：`AuroraTableContractTests` 三条。
  `Columns_AreReorderableButTheStarColumnStaysLast` 两种拖法都要试：
  拖星号列自己、以及把别的列拖到它右边。只试一种的话，
  另一种会以「表格右边空出一条缝」的形态活很久。
  `Columns_RestoreTheRememberedOrderAndTolerateADeclarationChange` 用一个
  已经不存在的列键钉住「声明变了不会把表弄坏」——这类记录是跨版本的，
  下一次改列声明时它是唯一会喊疼的地方。

- **真机已验**（2026-08-29 19:24 部署 1.9.2 候选包后，经回环网关 `/api/command` 实测）：
  `aurora.ui.data view=commands` 回 **187 行**——这一条同时就是 REQ-UI-057 的真机判据，
  因为目录会话没接上时它现在返回的是失败而不是空表；
  `view=domains` 回「全部 + aurora / diana / janus / mercury / portunus / vulcan」；
  `view=classes domain=janus` 回「全部 + core / github / gitrule / graph / history / proj / ui」；
  域筛选 janus → 41 行（与 Janus 自己门禁报的 41 条指令对上），
  再叠类筛选 `class=proj` → 16 行。两级联动与求交在真实注册表上都成立。

- **真机待验**（只能靠肉眼，门禁与回环探针都到不了）：
  1. 拖动列头的手感，以及星号列被拨回最右那一下在视觉上会不会显得突兀；
  2. 面板收薄之后，Janus 项目操作面板四行的实际高度与可读性；
  3. 折行阈值：真机窗口宽度下 Janus 第三行（提交描述 + 两个按钮）是否过早折行；
  4. 指令详情页顶部新加的那个控制面板（填入 / 复制示例 / 运行）三个按钮的启停。

- **探针路径的一条坑，记下来免得下次误判**：从回环网关打 `aurora.*` 会时灵时不灵，
  报「未知指令」。那不是页面坏了——`aurora.*` 本来就**不常驻宿主注册表**
  （`vulcan.module.list` 里 Aurora 的 `commandCount` 长期是 0，1.9.1 时也是），
  而页面走的是界面自己的总线，`ShouldUseRemote` 在宿主没有该指令时就地执行（1.8.13）。
  探针能打通的只是 Attach 后、命令被按 owner 回收前的那一小段窗口。
  **判页面死活不能靠这条路**，要么看界面，要么看界面自己的日志。

## 1.9.1 Release Checks

- `VERIFY-POPUP-TRIGGER`（REQ-UI-056）：`PopupTriggerContractTests` 六条。
  三条是这一版的核心判据：
  `AContextTriggeredPopupTakesNoLayoutSpaceAndOpensOnRightClick` ——**必须真的建窗口
  并把浮层打开**。只断言"渲染出了一个 AuroraFlyout"的话，这条会以全绿的形态空转：
  写这一版时第一稿把浮层收成了 `Collapsed`，WPF 的 `Popup` 在不可见的父级下开不出来、
  不抛异常也不触发 `Closed`，`IsOpen` 写进去就是 `false`——**除了真的去开一次，
  没有任何地方会说出这件事**；
  `AHandledRightClickDoesNotAlsoOpenTheFlyout` ——钉住"订阅的是冒泡事件且不收已处理的
  那些"，这是与表格行操作菜单共存的**全部**机制，失效的形态是右键行时弹出两层东西；
  `OnlyTheFirstContextPopupOnAPageIsWiredUp` ——第二个必须渲染成写明原因的牌子，
  静默少一个浮层是看不出来的。

- **真机待验**：右键落点的实际手感——空白区够不够大、浮层弹出位置是否被窗口边缘裁到。
  门禁只驱动路由事件，不做真实命中测试。

## 1.9.0 Release Checks

- `VERIFY-LAYERS`（REQ-UI-049）：`LayerContractTests` 三条——依赖只向下、
  每个源文件都有层、基础层不认识任何组件。
  **写它的时候一条违规都没有**，这是刻意的：它拦的是以后，不是现在。
  两条实现上的判据不能省：**先剥注释**（`AssemblyInfo.cs` 通篇在讲停靠系统，
  一个 `using` 都没有却提了 `HistoryAurora.Shell.Docking` 三次，不剥就是三条假违规，
  而误报的门禁最后一定被删掉），以及**不能只扫 `using`**（`PageDescription` 里的
  `HistoryAurora.Shell.Panels.PanelWidget` 是全限定写的，只扫 using 会被整个绕过）。
  「每个文件都有层」那一条同样不能省——没有它，加一个没人分层的文件夹就等于绕过整套规则。

- `VERIFY-PAGE-SCROLL`（REQ-UI-050）：`PageScrollContractTests` 两条。
  扫描前必须**泵到模板套上为止**并断言至少找到一个 `ScrollViewer`——表格的滚动视图
  来自 `ListView` 模板，不套模板一个都找不到，那时扫出来的「零违规」是假的，
  这条用例会以全绿的形态空转。
  判据是 `TemplatedParent == null` 再排掉自带视口的组件（白名单），
  **不是**「祖先里有没有表格」：第一版按后者判，`TextBox` 的 `PART_ContentHost`
  与下拉框弹出层当场报成违规。

- `VERIFY-PAGE-INSET`（REQ-UI-051，判据加强）：
  `Reload_GivesModulePagesTheSameInsetAsShellPages` 此前只查模块页这一侧，
  名字却写着「与自持页一致」——而 1.8.18 的真实情况是自持页 8、模块页 12，
  用户一眼看出了差别，这条全绿的用例一个字都没说。现在改为**与
  `PageRegistrar.Inset` 的输出逐项对比**（Padding 与 ClipToBounds），
  任一路单独改动立刻红。

- `VERIFY-HOSTED-PAGES`（REQ-UI-052 / 053）：
  `CommandPagesContractTests` 七条 + `ModulesPageContractTests` 三条。
  其中两条是这一版的**核心判据**：
  `EveryHostedPageParsesAndRendersWithoutMissingComponents` ——自持页出现缺件
  说明界面自己的协议表达不了界面自己的页面，1.9.0 的整条论证靠它在缺件出现时立刻变红；
  `EveryHostedActionResolvesToARegisteredCommand` ——断链的形态是「按钮点了没反应」，
  在界面上与「还没选中」完全一样，必须在这里判，不能等人去点。
  取数这一半现在**完全脱离 WPF**，那是把取数从视图挪进指令的附带好处。

- `VERIFY-HOSTED-PAGES` 追加（REQ-UI-055）：
  `TheCommandListFillsUpBeforeAnybodyTypesInTheSearchBox`。
  这条是**先红后绿**写出来的——把命令集改成描述式之后，页面一打开是空的，
  因为搜索框发布的初值是空串而取数把空串当成「还没选」。
  用例必须**真的建窗口并泵到表格填上**：只判「取数命令返回了行」的话，
  取数压根没发出去这件事一个字都测不到，而那正是缺陷所在。

- **真机待验（本轮无法在门禁内覆盖）**：
  1. 四页的实际观感——间隙是否真的与模块页一致、裁切有没有裁掉本该看见的内容；
  2. 命令集删掉两级联动与补全之后，搜索是否够用；
  3. 「热重载」两步指令在真宿主上的成败提示是否仍然可读。
  这三条只能开起来看，门禁给不了结论。

- **本轮记账、未做**：`VERIFY-PAGE-VIEW-OPTIONS` 不存在，因为 `view.filterable` /
  `sortable` / `selection` 至今**没有任何实现**（REQ-UI-054）。这是一张欠条，
  不是一条已完成的要求。

## 1.8.18 Release Checks

- `VERIFY-DISCOVER-COALESCE`（REQ-UI-048）：
  `CoalescingAsyncWorkContractTests` 钉住重叠请求共享同一个任务、失败后闸门仍能再用；
  `ModuleCommandProbeContractTests` 钉住动作与页面两次探测只打一次 `vulcan.command.list`；
  `CommandCatalogContractTests.Refresh_CoalescesOverlappingRemoteCatalogFetches` 钉住
  命令集刷新同样合并；
  `SelectionChannelContractTests.PublishingTheSameRowAgainDoesNotRaiseChanged` 钉住
  通道值没变不再通知。窗口能否先于发现出现，只能真机看 Show 是否还被 Normal 优先级
  的发现堵住。

## 1.8.17 Release Checks

- `VERIFY-PANEL-CHANNEL`（REQ-UI-045）：`RotatingTheOptionBoxSwapsTheComponentsBelowIt`
  同时钉住两件事——**初值那一支在页面打开时就已经在**（不发初值的话，界面停在
  「还没选」而选项框里明明写着一个值），以及转动选项框会真的换掉下面那批组件。
  外加 `TheSecondPublisherClaimingAChannelIsRefusedAndStaysSilent`：抢注失败的框
  不但要出声，改值时**还不能再往通道上写**——只判日志的话，那条静默的写入照样发生。

- `VERIFY-PAGE-SWITCH`（REQ-UI-046）：`ABranchNobodyOpenedNeverFetches` 是这条实现
  选择懒挂树而不是 `Collapsed` 的**唯一判据**——折叠元素照样收 `Loaded`，
  改成 `Collapsed` 的实现在别的所有断言上都能过，只有这条会掉。
  `SwitchingBackReusesTheSameComponentWithoutRefetching` 两条断言缺一不可：
  只判「同一个实例」的话，重取了一遍的实现也能过；只判「不重取」的话，
  换了一个新表格但恰好没触发取数的实现同样能过。
  另加 `ASwitchWithoutAUsableSourceSaysWhyInsteadOfGoingBlank`（声明错误**不算缺件**，
  否则模块会被自动记账去申请一个已经交付的组件）与
  `DuplicateAndUnreachableBranchesAreReportedOutLoud`。

- `VERIFY-PAGE-INSET`（REQ-UI-047）：`Reload_GivesModulePagesTheSameInsetAsShellPages`。
  判 `Padding` 等于 12，外加 **`ContentFactory` 两次调用返回同一个元素**——
  每次现包一层的实现在第一条断言上完全能过，代价要等到窗口被重建时才现形：
  内容被摘到新的 Border 上，上一处当场变空白。

## 1.8.16 Release Checks

- `VERIFY-SELECTION-CHANNEL`（REQ-UI-041）：`SelectionChannelContractTests` 四条——
  跨页驱动、两个占位符命名空间分得开、通道重名出声拒绝、断链出账。
  **跨页那条的判据是两次独立渲染**：同一次渲染里的节点表本来就通着，
  在一页里测出来的东西对"跨不跨页"一个字都没说。

- `VERIFY-STACK-GREEDY`（REQ-UI-042）：`ATableInsideAStackGetsTheLeftoverHeightAndActuallyScrolls`。
  两条断言缺一不可——只断「被限住」的话把表格设成固定高也能过，
  只断「能滚」的话撑满整页但内部恰好滚得动的实现同样能过。

- `VERIFY-PANEL-INLINE-ROW`（REQ-UI-043）：`AnInlineButtonSharesItsRowWithTheControlBeforeIt`
  判 Grid 行号相等而不是「看起来在一起」——按钮独占一行时版面上也挨着上一行，肉眼分不出来。
  外加 `AnInlineButtonWithNothingBeforeItIsRejected`。

- **本轮撞出一处既有缺陷并一并修掉**：动作占位符正则是 `\w+`，而 `\w` 不含连字符。
  `{project-name}` 这类控件 id **根本没被识别为占位符**——不替换、也不报「引用了不存在的控件」，
  原样上了总线。写这一轮的用例时才发现：期望 `new=新 名字`，实际拿到 `new={project-name}`。
  它在 1.8.16 之前就存在，且组件测试页与 Janus 的既有描述里都有连字符控件 id。
  已加正对照用例 `BuildCommandText_AcceptsHyphenatedAndDottedPlaceholderNames`。

- **`stack` 的渲染类型变了**（`StackPanel` → `Grid`），
  `PageProtocolContractTests.Render_MapsKnownComponentsToAuroraControls` 随之改断言，
  并补了三条行档位断言。没有表格/泳道的 `stack` 版面与原来一致。

- `VERIFY-DATA-REFRESH`（REQ-UI-044）：`ADataSourceThatReferencesTheSelectionRefetchesWhenItChanges`
  钉住「换选中就重取」，`RefreshDataDispatchesByPageAndSaysSoWhenNothingMatched`
  钉住按页/按节点分派**且不误伤别页**（刷新是有代价的：Janus 的项目表会去扫 45 个仓），
  `DroppingAnOwnerAlsoDropsItsDataBindings` 钉住撤页连带撤绑定。

- 消抖那条（`RapidSelectionChangesCoalesceIntoOneFetch`）判的是「只取一次**且取的是最后那个**」。
  只判「合并成一次」的话，取了第一个就停下的实现也能过，而那正好是错的那一半。

- **消抖这条的动机要说准**：真机日志里键盘翻项目的选中间隔是 450~600ms、
  每条取数 120~220ms 返回，那个速度下它跟得上，**没有观察到堆积**。
  最初写这条时把它记成了「十五条 Git 工作在排队」，那是从"三条取数 × 翻得快"
  推出来的，不是量出来的。消抖仍然做，但它是防御（按住方向键、程序重设选中），
  不是在修一个已发生的故障。

- 门禁：Release 0 警告 0 错误；`Contracts` 242 全绿。

- **未做**：真机目视；未跑 `Test-QualityGate.ps1` 之外的手工核对。

- 沿用 1.8.15 及以前各项。

## 1.8.15 Release Checks

- `VERIFY-TABLE-PROPORTION`（REQ-UI-039）与 `VERIFY-SWIMLANE-HEAD`（REQ-UI-040）：
  `TableProportionContractTests` 三条，外加沿用 `TableWidthContractTests` 的无横向滚动条断言。
  **两条正对照都已跑**：把列宽改回固定像素 → 缩放用例变红；去掉默认滚到最右 → 泳道用例变红。
  表格用例连跑三遍稳定通过（复算依赖布局轮次，必须确认不是偶然）。

- **本轮的自证错误**：收紧量一开始按「内容宽 － 视口宽」**累加**，结果同一份框架开销被扣了
  两次，右边空出 12px——正是这条需求要消灭的东西。改成每轮按「内容宽 － 列宽之和」重量，
  不累加。另外复算轮数从 3 提到 5：每轮只读得到上一轮布局的 `ActualWidth`，
  列数多且有行操作列时三轮不够，实测会漏掉 6px。

- **列宽语义变了，对模块是行为变更**：声明的数字从像素改为权重。
  已同步 `b-Office/package/HistoryAurora_组件清单与用法.md`。
  已有页面不会报错，但列宽表现会变——原先写死 230px 的列现在会随页面缩放。

- 门禁：Release 0 警告 0 错误；`Contracts` 230 / `ModuleSmoke` 3 / `Smoke` 1 全绿；
  `Test-QualityGate.ps1` 通过。

- **未做**：真机目视。

- 沿用 1.8.14 及以前各项。

## 1.8.14 Release Checks

- `VERIFY-PAGE-NODE-SET`（REQ-UI-038）：页面节点收敛为 7 种；
  `button` / `input` / `select` 被拒绝、渲染成写明原因的牌子、记 Warn，
  且**不进缺件表**（否则会自动变成一条组件申请）。
  用例：`RetiredNodeTypes_AreRejectedWithoutBecomingAComponentRequest`（三种各一例）。

- **这是对模块公开的破坏性协议变更。** 已同步改写
  `b-Office/package/HistoryAurora_组件清单与用法.md`（顶部加变更通告，三处示例改写为面板写法）
  与 `b-Office/current/页面注册协议方案.md`（迁移表标注退役，范例页面标注勿照抄）。

- **能力损失，非等价替换**：页面按钮的 `invoke` / `enabledWhen` 一并退役，
  「表格选中行 → 按钮可用」这条链路没有了；`enabledWhen` 在面板里没有对应物。
  原 `Render_TableLoadsRowsThroughBusAndFeedsSelectionIntoInvoke` 拆去按钮那半段，
  改名 `Render_TableDefersItsDataUntilTheControlLoads`，只保留 REQ-UI-030 的延迟取数断言；
  `Render_ButtonInvokeReachesTheCommandBus` 与
  `Render_DanglingEnabledWhenDisablesButtonAndWarns` 删除。

- **仓外影响未评估**：别的模块（如 Mercury）的页面描述不在本仓，
  若已使用顶层 `button` / `input` / `select`，升级后那些节点会变成牌子。
  本轮没有也无法在本仓验证这一点。

- 门禁：Release 0 警告 0 错误；`Contracts` 227 / `ModuleSmoke` 3 / `Smoke` 1 全绿；
  `Test-QualityGate.ps1` 通过。

- 沿用 1.8.13 及以前各项。

## 1.8.13 Release Checks

- **1.8.11 的修法无效，本轮换掉。** 宿主进程 14:56:35 重启、已装载 1.8.11，用户复测
  `✗ 未知指令: aurora.preview.rows / .graph` 依旧。因此「把登记提前到 Attach 之前」
  不是原因。改为 `VERIFY-COMMAND-ROUTING`（REQ-UI-036）：本机有、宿主没有的命令就地执行。
  这是严格收窄，只会减少「未知指令」，不会新增失败。
  用例：`CommandsTheHostDoesNotHaveStayLocal`，同时钉住「宿主有的照旧发过去」
  与「本机没有的照旧发给宿主」两条不变量。

- `VERIFY-DIVIDER-PIXELS`（REQ-UI-037）：`HorizontalPanelActuallyPaintsItsSeparators`。
  按渲染像素验收，只数列间空档；收窄重排后再验一次。

  **本轮的两个自证错误，记下来：**
  ① 判据先写成「整块有不透明像素」，子控件文字就能让它通过——收紧到只数空档后立刻变红。
  ② 变红之后我先归因成「OnRender 没跟着重排重跑」，加了一套按排版签名排队重画的机制。
  实际原因是 `RenderTargetBitmap.Render` 保留元素相对父级的偏移，我数错了坐标。
  偏移修正后用例转绿；把那套重画机制**摘掉后用例照样绿**，因此已删除——
  基于错误归因加的机制不能因为"反正无害"就留在仓里。

- 控制台过滤器工具条原先三条 `Aurora.Segment.Divider` 全是 `Visibility="Collapsed"`，
  且 级别/域/类 之间根本没有分隔件——**「控制台一条渐隐线都没有」属实**。
  本轮补上，并给该样式加上与面板同一套的渐隐遮罩；`Aurora.Panel.VerticalDivider` 删除。

- 门禁：Release 0 警告 0 错误；`Contracts` 228 / `ModuleSmoke` 3 / `Smoke` 1 全绿；
  `Test-QualityGate.ps1` 通过。已部署。

- **未做**：真机复测。这一轮同样要你在运行的界面上确认。

- 沿用 1.8.12 及以前各项。

## 1.8.12 Release Checks

- `VERIFY-PANEL-SURFACE-SINGLE`（REQ-UI-035）：`PanelSurfaceAndConsoleToolbarShareOneStyle`。
  控制台过滤器工具条与控制面板底板必须解析到同一个键，且 `Aurora.Segment.Bar` 已不存在。
  **正对照已跑**：把控制台那个 Border 换成别的样式，用例报
  「控制台过滤器工具条没有用面板底板那份样式——同一种东西又变成了两套边距」。

- 数值：底板 `Padding` 4 → 2；面板内容内缩 8 → `6,4`；横排列间距 16 → 12、行距 12 → 8、
  列宽下限 132 → 112。首个控件左边距因此落在 8px，与控制台工具条的标签左边距对齐。

- 门禁：Release 0 警告 0 错误；`Contracts` 226 / `ModuleSmoke` 3 / `Smoke` 1 全绿；
  `Test-QualityGate.ps1` 通过。已部署。

- **未做**：真机目视。本轮改的是边距数值，测试宿主不加载完整主题，位图核对不了这一项，
  需要你在运行中的界面上看。

- 沿用 1.8.11 及以前各项。

## 1.8.11 Release Checks

- **1.8.10 的 `VERIFY-GALLERY-SELF-CONTAINED` 判据有误，本轮更正。** 那一轮把取数指令
  挪到「页面打开前登记」，并用「注册表里有」当判据。真机实测 `✗ 未知指令: aurora.preview.rows`
  与 `aurora.preview.graph` **依旧存在，且比 1.8.9 更糟**（1.8.9 只有 graph 失败）。
  原因是界面总线默认把命令发给宿主，而宿主注册表只收 Attach 那一刻发布的那批——
  进程内用例没有 RemoteExecutor，查注册表永远通过，**查不出这条不变量**。
  现判据改为 `FrontendCommandCatalog.FrameworkSourceDescriptors`（即会被发布到宿主的那批）。
  **正对照已跑**：删掉 `BuiltinCommands.Register` 里那一行，
  `DataCommandsAreInTheSnapshotThatGetsPublishedToTheHost` 变红。

- `VERIFY-PANEL-BOARD`（REQ-UI-034）：`HorizontalPanelWrapsIntoRowsAndColumns` +
  `PanelNeverPutsItsContentInAScrollViewer`。前者按「窄宽度下 DesiredSize 不超出可用宽度」
  与「放宽可用宽度后高度变矮」判定真的排成了多行多列，后者断言两个方向零 `ScrollViewer`。

- **人工目视（已做）**：在测试台把整页渲染成位图（1100×820 与 700×900 两种宽度）核对过——
  表格 9 行、泳道 9 节点均有数据；面板在两种宽度下分别排成 3 列与 3 列多行，列宽对齐，
  行列之间可见渐隐分隔线，无滚动条。**注意该位图不代表真机外观**：测试宿主没有加载完整主题，
  按钮与面板底色均为默认样式，因此只用于核对**几何**，不用于核对配色。

- 门禁：Release 0 警告 0 错误；`Contracts` 225 / `ModuleSmoke` 3 / `Smoke` 1 全绿；
  `Test-QualityGate.ps1` 通过。本轮**已部署**到 `%AppData%\HistoryVulcan\Modules\HistoryAurora\`。

- 沿用 1.8.10 及以前各项（当时 REQ-UI-025 布局持久化尚未动，已由 1.12.20 的 REQ-UI-067 关闭）。

## 1.8.10 Release Checks

本轮修的是 1.8.9 引入的三处回归。三条都做了**正对照**：先把修复撤掉、确认用例变红，
再装回来、确认变绿。没跑正对照的用例不计为已验（见 1.8.2 的欠账记录）。

- `VERIFY-CHROME-DRAG`（REQ-UI-031）：`MainChromeHeaderStartsAWindowGestureEvenThoughThePaneIsASelector`。
  判据是「这一下被标题栏收下了」——手势收下会把事件标成已处理，判成「点在控件上」则原样放行。
  **不查** `_pendingHostWindow`：那一步还要 `CaptureMouse`，测试里没有真实鼠标，捕获失败会
  立刻反手清空手势，查它等于在查捕获而不是在查命中判定。
  **正对照已跑**：把判据换回 `Selector`，用例报「主窗口标题栏没有接下这一次按下，窗口将拖不动」。

- `VERIFY-PANEL-ORIENTATION`（REQ-UI-032）：`PageLevelPanelKeepsTheDeclaredOrientationAndItsFadeSeparators`。
  断言页面级面板保持 `Orientation.Horizontal`、两条分隔线各有 `Width=1`、非空 `OpacityMask`
  和大于 0 的 `MinHeight`。
  **正对照已跑**：删掉 `BuildPanel` 里那一行 `Orientation`，用例变红。

- `VERIFY-GALLERY-SELF-CONTAINED`（REQ-UI-033）：`ComponentGalleryContractTests` 三条。
  整页渲染按**日志**判定——两处故障都不抛异常，页面照样画出来，只是画的是一排警示牌，
  所以判据是「Warn 及以上一条都没有」。
  **正对照已跑**：去掉 `DeclareLocal`，用例报出五条
  「未声明的动作: preview.row / preview.apply ×4」，与真机症状一致。

- 门禁：Release 构建 0 警告 0 错误；`Contracts` 223 条、`ModuleSmoke` 3 条、`Smoke` 1 条全绿；
  `Test-QualityGate.ps1` 通过。

- **未运行**：真机验证。本轮按用户要求**只提交、不部署**（其他开发正在进行），
  因此三项修复均未在运行中的宿主里跑过。上述结论仅覆盖门禁范围。

- **沿用的欠账**：1.8.2 的 `VERIFY-DOCK-PROBE` 正对照仍未跑（依赖会抢物理光标的鼠标冒烟）；
  布局持久化 REQ-UI-025 当时仍未修，已由 1.12.20 的 REQ-UI-067 关闭；1.8.2 探针欠账不变。

- 沿用 1.8.3 及以前各项。

## 1.8.3 Release Checks

- `VERIFY-PAGE-DATA-LIFECYCLE`（REQ-UI-030）：`table` / `swimlane` 在页面 Render 阶段不得执行
  `dataSource`；控件首次 Loaded 后才启动一次取数。模块命令在线程池执行，结果回到 UI 线程更新组件。
- `PageProtocolContractTests.Render_TableLoadsRowsThroughBusAndFeedsSelectionIntoInvoke` 必须证明 Render 后
  命令记录为空，触发 Loaded 后才取得行，并保持选中行参数绑定可用。
- 沿用 1.8.2 及以前各项。

## 1.8.2 Release Checks

- `VERIFY-DOCK-PROBE`（REQ-UI-028）：新增 `有画刷 / 无画刷样本` 与覆盖窗自身的
  `不透明度 / 底色 / 透明窗`，用于分辨"画刷根本没解析到"与"画刷有了却没画上"。

  **正对照：未运行。** 该正对照依赖 `FloatingDockMouseSmoke`，而它驱动物理光标，
  本机当前有人使用，运行会干扰使用者桌面（见 1.8.1 记录）。因此本轮新增的两条结论
  分支**尚未在已知能合并的链路上验证过不会误报**——按 REQ-UI-028 这是硬性要求，
  此处属**已知欠账**，待本机无人值守时补跑。不得因门禁 209 条全绿就当作该项已验。

  **真机（人工，未运行）**：待回传。1.8.1 真机实测 `覆盖窗字典 0→1`，主题按实例
  下发已生效；`实绘像素` 仍为 0，故问题已收敛到"画刷是否解析得到"这一步。

- 沿用 1.8.1 及以前各项。

## 1.8.1 Release Checks

- `VERIFY-DOCK-OVERLAY-THEME`（REQ-UI-029）：主题改为按实例下发（`DictionaryTheme`）。
  门禁 209 条全通过；其中 `ShellChromeContractTests` 9 条是本条的守门人——只改覆盖窗
  那一处、漏改 `LoadThemeResources` 时，它们全红。

  **真机（人工，未运行）**：待回传 `覆盖窗字典` 与 `实绘像素`。真机 1.8.0 实测
  `覆盖窗字典=0`，已坐实根因。

- **`VERIFY-MOUSE-DOCK` 记录更正。** 1.8.0 记的"约每 3～4 次偶发"**判断有误**：
  该冒烟驱动的是**物理光标**，失败源于两件真实的事，不是随机偶发——
  ①人在旁边动了鼠标；②冒烟自身的测试窗摆位会算出**负坐标**抓取点
  （实测 `grab=(-794,692)`），点击因而落到使用者真实桌面上正在运行的宿主窗口里。
  后者是冒烟的缺陷，也是它**不得在有人使用本机时运行**的原因。摆位与屏幕边界校验
  未修，留作待办；在此之前该冒烟只在无人值守时运行，其失败不得直接计为产品缺陷。

- 沿用 1.8.0 及以前各项。

## 1.8.0 Release Checks

- `VERIFY-DOCK-OVERLAY-THEME`（REQ-UI-029）：拖动浮窗时覆盖窗须补挂主题字典，日志出现
  「已按 DEC-009 给覆盖窗补挂主题字典」，且 `覆盖窗实绘像素` 大于 0。

  **门禁（自动，已运行）**：基线不变，`实绘像素=1044/52250`、`覆盖窗字典=1`。门禁本就
  能解析到画刷，故此项在门禁里**证伪不了也证实不了**——它只保证补挂不会让原本正常的
  链路变坏。真机才有判别力。

  **真机（人工，未运行）**：待用户回传 `覆盖窗字典` 与 `实绘像素`。

- `VERIFY-MOUSE-DOCK` **已知偶发**：`FloatingDockMouseSmoke` 实测约每 3～4 次失败一次
  （`与命令集同一窗格=False`，且无探针输出，即拖动手势整个没起来）。合成输入的时序问题，
  在 1.7.7 加扩展探针时即已出现，**早于** 1.8.0 的覆盖窗改动。尚未定位，不得因其偶发
  就当作通过；判定以连跑三次至少两次通过为准，并在交付时说明。

- 沿用 1.7.9 及以前各项。

## 1.7.9 Release Checks

- `VERIFY-DOCK-PROBE`（REQ-UI-028，扩到第 6 环）：新增一行
  `覆盖窗实绘像素 / 主色 / Z序`。

  **正对照（自动，已运行）**：`FloatingDockMouseSmoke` 得
  `实绘像素=1044/52250 主色=#36000000 Z序 覆盖窗=18 主窗体=22`，结论「六环齐全」。
  此即"指示确实画在屏幕上且不被遮挡"的基线。

  同一正对照证伪了按 `Image.Source` 判断的做法：门禁里 `覆盖窗图像=0`，指示不是图片画的。

  **真机（人工，未运行）**：待用户回传该行与结论。真机 1.7.8 实测五环全过
  （元素 210 / 可见 105 / 具名投放件 16，均高于门禁基线）却无任何指示，故第 6 环是
  本轮唯一有判别力的一环。

- 沿用 1.7.8 及以前各项。

## 1.7.8 Release Checks

- `VERIFY-DOCK-PROBE`（REQ-UI-028）：新增一行
  `光标进过停靠区 / 最近距离 / 末位置`，用于判定"到底有没有拖进停靠区"。

  **正对照（自动，已运行）**：`FloatingDockMouseSmoke` 得
  `光标进过停靠区=是 最近距离=0px 末位置=(1101,602)`，结论「五环齐全」。

  同一正对照证伪了一条结论分支：WPF 口径坐标与真实光标偏差最大 956px 且全程未进停靠区，
  合并仍成功，故 AvalonDock 用的是真实光标。该分支已删除。

  **真机（人工，未运行）**：待用户把浮窗拖进主窗体中央后回传该行与结论。

- 沿用 1.7.7 及以前各项。

## 1.7.7 Release Checks

- `VERIFY-DOCK-PROBE`（REQ-UI-028，扩到第 5 环）：探针须再报一行
  `停靠区 / 覆盖窗元素 / 其中可见 / 具名投放件 / 最终投放目标`。

  **正对照（自动，已运行）**：`FloatingDockMouseSmoke` 连跑两次，均为
  `停靠区=2 覆盖窗元素=210 其中可见=80 具名投放件=12 最终投放目标=DocumentPaneDropTarget`，
  结论「五环齐全」。此即"指示正常画出"的基线。

  **真机（人工，未运行）**：待用户回传该行与结论。

- 沿用 1.7.6 及以前各项。

## 1.7.6 Release Checks

- `VERIFY-DOCK-PROBE`（REQ-UI-028）：浮窗拖动结束时，`shell.chrome` 必须输出三行——
  四环计数、四环状态、以及一条指名断点的结论。

  **正对照（自动）**：在 `FloatingDockMouseSmoke` 这条已知能合并的链路上运行，结论必须
  是「四环齐全」。本轮实测：`ENTERSIZEMOVE=1 WM_MOVING=17 EXITSIZEMOVE=1`、
  `DragService=已建 覆盖窗宿主=1 覆盖窗=模板=有 位置=(730,320) 尺寸=1100x760`。
  探针首版在同一条链路上误报「断在第 4 环」（点采样过早），修正为全程观测后才转正——
  **这个正对照必须每轮跑，读不准的探针比没有探针更糟**。

  **真机（人工，未运行）**：需要用户在真机上拖一次浮窗并回传这三行。真机环境无法在
  门禁中复现（模块在宿主里进的是可回收 ALC，测试进程不是），因此本项在交付时只能标
  「待真机确认」，不得写成已通过。

- 沿用 1.7.5 及以前各项。

## 1.7.5 Release Checks

- `VERIFY-MOUSE-DOCK`：`FloatingDockMouseSmoke`（真实鼠标输入）。
  浮出工具窗口 → 按住页头拖过主窗体 → 落在中央文档区中央 → 松手 → **合并成页签组**。
  五条断言按链路顺序：走到 `DragMove` → `IsDragging` 出现 → 浮窗数归零 →
  回到主布局 → 与命令集同一窗格。**2026-08-25 实测全绿，连跑两次稳定**——
  也就是 1.7.4 那个修法（拖动前放掉 Win32 鼠标捕获）在代码层面是成立的。
  **默认不跑**（会接管物理鼠标），需 `AURORA_MOUSE_SMOKE=1`。
- 顺带加的真机诊断：`TryDragWindow` 现在在进入拖动前后各记一条
  `[shell.chrome] 开始拖动窗口 …` / `窗口拖动结束`。
  真机上浮窗拖不动时，先看这两条在不在——不在就说明手势压根没起来，
  在而没有蓝色指示才是移动循环的问题。
- 全量：`Contracts` **209** 条（含冒烟的跳过分支）+ `Smoke` 1 + `ModuleSmoke` 3 全绿。

## 1.7.4 Release Checks

- `VERIFY-FLOAT-DOCKBACK`（第三轮，前两轮的判断已作废）：
  反编译确认 `LayoutFloatingWindowControl.FilterMessage` 只看
  WM_SYSCOMMAND(仅最大化/还原) / WM_LBUTTONUP / **WM_MOVING** / WM_EXITSIZEMOVE，
  不看任何非客户区消息；并用反射直接调 `UpdateDragPosition` 验证过
  `_dragService` 与 `DockingManager._overlayWindow` 都能建起来——
  **停靠机制本身是好的**，缺的是"窗口真的被系统移动循环拖着"。
  修法：`DragMove()` 之前放掉 Win32 鼠标捕获（`ReleaseCapture`）。
  `CaptionHeight` 回到 0，`WM_NCLBUTTONDOWN` 的做法撤销。
- 全量：`Contracts` **208** 条 + `Smoke` 1 + `ModuleSmoke` 3 全绿。

**未运行，原因写明**：拖放手感只能真机确认——自动化驱动不了 Win32 移动循环。
判断标准：拖着浮窗经过主窗体时，那组蓝色方位指示出不出来。

## 1.7.3 Release Checks

- `VERIFY-FOCUS-NO-XML`：`MaximizeContractTests`（8 条）。聚焦与还原不经 XML 往返；
  聚焦前那棵 `LayoutRoot` 被留住。**根因已用探针证实**：对可回收 ALC 里的类型做
  `XmlSerializer.Serialize` 报「非可回收程序集不能引用可回收程序集」，
  而 AvalonDock 随模块包装进那个上下文。
- 全量：`Contracts` **208** 条 + `Smoke` 1 + `ModuleSmoke` 3 全绿。

**未运行 / 未修，原因写明**：
- **当时未修，现已关闭**：`%AppData%\HistoryVulcan\layout` 在 1.7.3 仍为空；1.12.20 已由
  REQ-UI-067 的 `layout.v1.json` 纯数据快照替换 `XmlLayoutSerializer`。
- 真机验收（双击标题不再报异常、浮窗能叠回去）**只能人工确认**。

## 1.7.2 Release Checks

- `VERIFY-FLOAT-DOCKBACK`（补齐）：浮窗页头的拖动改为向窗口发
  `WM_NCLBUTTONDOWN`/`HTCAPTION`，AvalonDock 据此创建 DragService。
  1.7.1 只把 `CaptionHeight` 从 0 抬起来（前提条件），但 Aurora 自己接管了页头拖动、
  仍旧走 `Window.DragMove()`，那条消息始终没发出去——**所以 1.7.1 装上去仍然叠不回去**。
  **本条只能真机确认**：判断标准是拖着浮窗经过主窗体时蓝色停靠指示出现、松手能叠回去。
- `VERIFY-TABLE-WIDTH`：`TableWidthContractTests`。按真机命令集那套列
  （指令 230 / 类 76 / 只读 48 / 说明 * + 两个行操作）在 584px 宽的表里渲染，
  断言内容宽度不超出滚动视口。**修之前这条会红**（视口 565、内容 568），
  与真机上那条横向滚动条是同一件事。另一条断言截断判定：
  放不下才算截断，放得下不算。
- 全量：`dotnet build` 零警告零错误；`Contracts` **207** 条 + `Smoke` 1 + `ModuleSmoke` 3 全绿。
- `VERIFY-DEPLOY-172`（2026-08-25 16:00 真机实测）：热重载成功
  （`16:00:27.265 ✓ 模块 HistoryAurora 1.7.2`，PID 28608 未重启）。窗口截图核对：
  **模块管理的「描述」列有内容了**（「前端界面：…」「AI 工作区巡…」「活动项目坞…」，
  1.7.0/1.7.1 上这一整列只有「前…」）；**命令集表格底部的横向滚动条不见了**。
  浮窗拖回停靠仍待人工确认。

## 1.7.1 Release Checks

- `VERIFY-FLOAT-DOCKBACK`：`FloatingWindowChromeContractTests`。两个浮窗控件的
  `WindowChrome.CaptionHeight > 0`（没有它，AvalonDock 收不到
  `WM_NCLBUTTONDOWN/HTCAPTION`，也就不创建 DragService，浮窗叠不回布局）；
  页签与窗格动作按钮的 `IsHitTestVisibleInChrome` 豁免仍在。
  `ShellChromeContractTests.FloatingToolWindowHasNoSecondOuterChromeRow` 里那条
  `CaptionHeight == 0` 已改正——它把"没有第二条标题行"与"没有标题命中区"当成了同一件事，
  而前者由模板保证，与命中高度无关。
- `VERIFY-SELF-PROBE`：`ActionDeclarationContractTests.Reload_DoesNotAskAuroraItself`。
  探测排除界面自己的域，`aurora.ui.actions`（查询命令）不再被当成动作声明去调。
- `VERIFY-DOCK-DIAG`：停靠命令抛出前把完整异常写进本端日志；
  `MaximizeWindow` 中途失败时清掉暂存布局（`MaximizeContractTests` 覆盖成功路径：
  逐个窗口最大化+恢复、有浮窗时、中央区有模块页时，均正常）。
- `VERIFY-COMPONENT-DICT`：控件字典按线程缓存。按进程缓存会让跨 STA 线程取用抛
  「调用线程无法访问此对象」（实测导致测试宿主崩溃），因此必须是 `[ThreadStatic]`。
- 全量：`dotnet build` 零警告零错误；`Contracts` **205** 条 + `Smoke` 1 + `ModuleSmoke` 3 全绿。

- `VERIFY-DEPLOY-171`（2026-08-25 15:28 真机实测）：1.7.1 经 `--install-module` 写入运行区，
  宿主（PID 16236，启动于 15:07:30）热重载：`15:28:39.832 ✓ 模块 HistoryAurora 1.7.1`。
  模块管理列出 `HistoryAurora 1.7.1`、47 条域指令；命令集 122 条。
  **`✗ 未知指令: aurora.ui.actions` 不再出现**，顶栏错误计数归零；
  日志里这一轮只剩 `mercury.ui.actions` / `mercury.ui.describe`，探测不再自调（REQ-UI-020 生效）。
- 另注：`--install-module` 写完文件后**目录监视两次都没有自行触发**
  （1.7.0 与 1.7.1 各一次），手动改一下 manifest 的时间戳才动。文件确实已经落盘。
  1.6.0 那次它是自己触发的，所以不是必现；成因未查清，记在此处。

**未运行，原因写明**：
- **拖放手感必须真机确认**。浮窗拖回停靠需要真实的 Win32 移动循环，
  自动化里驱动不了；门禁只能钉住"标题命中区存在"这个前提条件。
- **`aurora.ui.max` 的 NotSupportedException 未能复现**。逐个窗口最大化、
  有浮窗时最大化、中央区有模块页时最大化，三种都试过且全绿。
  本轮的做法是让它下次发生时能被定位：异常全文会进 Aurora 自己的控制台
  （总线只外发类型名，这是刻意的），另外把最可疑的一条路径——组件构造时按 URI
  取控件字典——改成按线程缓存且不抛。**这是推断，不是已证实的修复**，
  下次双击若仍报错，控制台里会有带堆栈的那一条。

## 1.7.0 Release Checks

- `VERIFY-ROWACTION`：`RowActionContractTests` + `PageComponentsContractTests` 全绿。
  一份声明出两个出口（行内按钮列 + 右键菜单），且行内**只多一列**而不是每条动作一列；
  `inline=false` 的只进菜单；触发时带的是被操作那一行而非选中行；
  没有行操作时右键不弹菜单；未声明的 id 不触发。
  页面侧：占位符 `{列名}` 默认取被点行的同名列；动作断链时表照画、上面加一块写清原因。
- `VERIFY-POPUP`：`popup` 不再渲染成缺件占位；内容复用面板的三种小组件与同一份校验。
- `VERIFY-SUGGEST`：`CommandCatalogContractTests` 的补全用例——按前缀补指令名并
  **替换**已打出的那一段；前缀无果时退回包含匹配；命令后补参数名且不重复已写的参数；
  `参数=` 之后补允许值且只替换等号右边。
  页面侧：声明 `suggest` 但无补全会话时退回普通输入框**并记 Warn**。
- `VERIFY-CATALOG`：`CommandCatalogContractTests` 全绿（全部用例只有一张本地注册表，
  即「没有任何模块时目录也成立」）。域/类/查询三级筛选；域回到「全部」时类同步收敛；
  `DetailAsync` 从本地注册表读出参数表，未知指令返回 null。
- `VERIFY-GRID`：`ResponsiveGridContractTests` 全绿。列数随可用宽度收敛（1000→4 列、
  700→3 列、260→1 列）；列数不超过子元素数；行不重叠。
- `VERIFY-PADDING-HEADER`：`TableHeaderContractTests`。`Aurora.GridHeader` 对
  `Role=Padding` 单独换**模板**（只改背景不够——右缘那条分隔线还在，看起来仍是一列）。
  这条同时顺带验证了 `AuroraComponentResources.Ensure` 真的解析得到控件字典。
- `VERIFY-DOCK-INTERNAL`：`DockingSurfaceContractTests`。
  `HistoryAurora.Shell.Docking` 命名空间零导出；`ShellWindow` / `ShellConfig` /
  `ShellCommandServices` 不在导出面上；`InternalsVisibleTo` 只授给
  `Smoke` / `Contracts` / `ModuleSmoke`。
- `VERIFY-INVALIDATE`：`PageInvalidateContractTests`。`ReloadOwnerAsync` 同时接受
  `HistoryMercury` 与 `mercury`；撤旧页用的是模块名；空名不做任何事。
  这条是 1.6.0 真机缺陷（需求书 D1）的回归门禁——此前本仓没有一条用例走这条路径。
- `VERIFY-CATALOG-PAGE`：`CommandPagesContractTests`。两页在真实 `Window` 里建起来并填上数：
  命令集列出注册表（**没有任何模块在场**）且带行操作；指令详情随
  `CommandSelectionState` 变化拉出参数表。这两页当初"消失"时没有任何门禁能发现——
  它表现为主文档区空着，不是一条报错。
- 全量：`dotnet build HistoryAurora.sln -c Release` 零警告零错误；
  `Contracts` **197** 条 + `Smoke` 1 条 + `ModuleSmoke` 3 条全绿。（2026-08-25 实测）

- `VERIFY-DEPLOY-170`（2026-08-25 14:36 真机实测）：1.7.0 经 `--install-module` 写入运行区，
  宿主（PID 2336，`HistoryVulcan v5.1.0`，启动于 13:13:24）**进程内热重载**，PID 与启动时间不变。
  宿主日志：`14:36:20.548 ✓ 模块 HistoryAurora 1.7.0 (精准暴露)`、
  `14:36:21.218 模块装载完成: 6 个模块,83 条指令`。窗口截图核对（深色）：
  - **命令集回到中央主区**，列出 120 / 120 条，域与类两级筛选在位，
    行操作列的「详情」「填入」两个按钮正常渲染；
  - **指令详情**停在右侧，未选中时显示「在命令集里选一条指令」；
  - 模块管理列出 `HistoryAurora 1.7.0`、47 条域指令，**「暴露」列已去掉**；
  - **表格最右侧不再多出一列空的**（`Role=Padding` 表头改用独立模板生效）；
  - Mercury 的「扩展坞管理」页仍在命令集旁边注册成功。

**真机上发现、尚未修的四条**（1.7.1 输入）：
1. 模块管理的「描述」列仍被挤成「前…」。去掉「暴露」只让出 80px，
   左侧停靠区实测约 370px，而固定列合计 312px——星号列仍然只剩几十像素。
   要么再减一列，要么给星号列一个"挤不下就换行/省略并加宽停靠区"的策略。
2. 命令集的表格出现**横向滚动条**：星号列算宽时行操作列的按钮外边距（每个 4px）
   没有计入，`InlineWidth` 的估算偏小，合计约超出 30px。
3. **一次热重载会触发 34 轮界面发现**（14:36:21.24→.47，230ms 内 104 次
   `vulcan.command.list`）。成因是 `hostBus.Registry.Changed` 每注册一条命令就排一次
   `DiscoverModuleSurfacesAsync`，而只有页面拉取器做了重入合并，动作台账与注解认领没有。
   最终状态正确，但这是启动期的一次无谓风暴。**此条 1.6.0 及更早即存在**，不是本轮引入。
4. 热重载窗口期有一条 `✗ 未知指令: aurora.ui.actions`（14:36:20.996），
   0.25 秒后同一条调用成功。这与 Mercury 报的 D1 是**同一条路径上的第二个故障**：
   D1 是 Aurora 把 owner 当域去拼命令名（已修，见 REQ-UI-018），
   而这一条是**模块在 Aurora 重新注册命令之前就发出了上行通知**——
   13:18:23 Mercury 的 `aurora.ui.invalidate` 被宿主判为未知指令，正是同一个竞态。
   模块每次热重载都可能撞上，需要与 Mercury 一起定：是 Aurora 排队待注册后重放，
   还是模块侧重试。

另注（与 Aurora 无关，但同一份日志里看得见）：`HistoryDiana 2.0.2` 与
`HistoryMinerva 4.4.5` 每次装载都因 `MissingMethodException`（`IModuleContext.get_Settings()` /
`get_DataDirectory()`，均为 5.x 已删除的成员）**接不上宿主，指令不注册**。
这正是模块管理页那一列「装载了但 Attach 失败」要显示的东西。

**未运行，原因写明**：
- **弹出层、补全输入框、响应式栅格、行操作的右键菜单仍未在真机上看过**：
  它们要有模块的页面描述才会出现，而当前唯一在用描述协议的 Mercury 还没有用到这几样。
  命令集页面里的行操作（行内按钮）是这一批组件唯一有真机证据的一处。
- **浅色主题目视仍未做**（与 1.6.0 同因：本机是深色）。
- 已知易受环境影响的一条：`ShellChromeContractTests.ConsoleLongLinesWrap…` 断言
  剪贴板复制成功，本机剪贴板被别的进程占用时会报「复制失败」。
  单跑两次均通过，与本轮改动无关（`CopySelected` 未被触及），但它确实是一条会闪的门禁。

## 1.6.0 Release Checks

- `VERIFY-TABLE`：`AuroraTableContractTests` 全绿。含结构断言——`AuroraTableColumn`
  的公开面只有 `Key` / `Title` / `Width`；行按列补齐，缺键为空串；列宽写错退回自适应。
- `VERIFY-PANEL`：`PanelComponentContractTests` 全绿。三种小组件之外一律拒绝；
  按钮无 `action` 即校验失败；动作未声明时渲染警示牌而**不是**禁用按钮；
  必填为空时拒绝执行并记错误。
- `VERIFY-ACTION`：`ActionDeclarationContractTests` 全绿。版本/冒名/重复 id 整份作废；
  断链进账；占位符引用不存在的控件时整条拒绝执行。
  `aurora.ui.actions` / `reloadactions` / `invoke` 已注册。
- `VERIFY-SWIMLANE`：`SwimlaneLayoutContractTests` 全绿。只给节点与父边即可排出泳道；
  子节点落在父右侧；边可由 `parents` 推导；节点自报的泳道优先于回溯；视口裁剪正确。
- `VERIFY-HOST-5`：`MinimumHistoryVulcanVersion` 为 5.4.0，且只在 `AuroraVersion.props`
  出现一次。5.0.1 → 5.1.0 的公开 API 基线逐条比对：Core / ServiceHost 零变化，
  Services 纯增量（`ModuleMeta.Attached` / `AttachFailures`）。
- 全量：`dotnet build -c Release` 零警告零错误；`Contracts` 164 条全绿；
  `Test-QualityGate.ps1` 通过。（2026-08-24 实测）

- `VERIFY-DEPLOY`（2026-08-24 23:50 真机实测）：宿主快照已重发为 5.1.0 并在跑
  （主窗标题 `HistoryVulcan v5.1.0`）。1.6.0 候选包经 `--install-module` 写入运行区后，
  宿主的模块目录监视自行热重载（进程未重启，PID 不变），模块管理列出
  `HistoryAurora 1.6.0`，控制台记录 `23:50:14 [page] 页面拉取完成`。
  表格组件在**深色**主题下渲染正确：表头次级色、行高一致、发丝线分隔、星号列占满剩余。

**未运行，原因写明**：
- **浅色主题目视未做**。本机当前是深色（`ui.theme=dark`），只核对了深色一半。
- 面板与泳道组件**未见真机渲染**：尚无模块注册 `<域>.ui.actions` 或返回含
  `panel` / `swimlane` 的页面描述，实测为「问了 0 个模块」。二者目前只有结构与行为
  经自动化覆盖，不得记作真机已通过。

## 1.5.2 Release Checks

- 沿用 1.5.1 的宿主 5.0 对齐与界面注解认领检查。
- 模块开发走宿主 `--cli vulcan.dev.start` / `submit` / `finish`。
- 2026-08-24 管线探测：从 `z-Publish/host/HistoryVulcan.exe` 拉起宿主后，因 1.5.1 历史包不可覆盖而升到 1.5.2，再走 start → submit。

## 1.5.1 Release Checks

- 沿用 1.5.0 的宿主 5.0 对齐与界面注解认领检查。
- 模块开发走宿主 `--cli vulcan.dev.start` / `submit` / `finish`。

## 1.5.0 Release Checks

- `VERIFY-UI-ANNOTATION`：`UiAnnotationClaimerTests` 全绿。带 `ui.window` 注解的命令
  被执行后，活对象进入停靠布局；重认领会先卸掉旧 owner。
- `VERIFY-HOST-5`：模块入口只实现 `IModuleContextAware`；`MinimumHistoryVulcanVersion` 为 5.0.0。
- 沿用 1.2.0 的弹窗与进程内确认检查。

## 1.2.0 Release Checks

- `VERIFY-DIALOG`：`AuroraDialogContractTests` 全绿。抽空应用级资源后弹窗仍能解析
  `Aurora.Brush.Surface`；深色令牌为 `#1D201F`；窗口 `WindowStyle.None` 且自绘顶栏标题可见；
  `aurora.ui.dialog` 已注册且无人值守测试不得调用 `ShowDialog`。
- `VERIFY-INPROCESS-CONFIRM`：`AuroraShellHost.WireBuses` 把宿主 `Confirmation` /
  `ConfirmationRouter` 接到 `MessageBoxConfirmation`。没有 WebSocket Shell 时写操作确认
  仍弹出 Aurora 确认框，不得静默拒绝。
- 沿用 1.1.0 的 `VERIFY-HOTRELOAD` 与 0.1.0 的构建/装载/门禁检查。

## 1.1.0 Release Checks

- `VERIFY-DIALOG`：`AuroraDialogContractTests` 全绿。抽空应用级资源后弹窗仍能解析
  `Aurora.Brush.Surface`；深色令牌为 `#1D201F`；`aurora.ui.dialog` 已注册且无人值守
  测试不得调用 `ShowDialog`。
- `VERIFY-HOTRELOAD`：manifest `pinned` 为 false；`DestroyUi` 在未 Attach 时不抛。
  真机热重载还要求已刷新的 HistoryVulcan 宿主快照（先拆界面再装新包 + Resolving 返回
  可回收 ALC 程序集）。未刷新宿主时不要把「未运行」写成「已通过」。
- 沿用 0.1.0 的 `VERIFY-BUILD` / `VERIFY-MODULE-LOAD` / `VERIFY-QUALITY-GATE` /
  `VERIFY-HOST-CONTRACT` / `VERIFY-VERSION-SINGLE-SOURCE`。

## 0.1.0 Release Checks

骨架版本，验收范围限于「仓库形态正确、能构建、能被宿主装载」。

- `VERIFY-BUILD`：`dotnet restore --locked-mode` 与 `dotnet build -c Release` 通过，
  `dotnet format --verify-no-changes` 无差异。
- `VERIFY-MODULE-LOAD`：`ModuleSmoke` 在测试宿主中装载 `HistoryAurora.dll`，
  `AuroraBusinessComposition.Attach` 被调用并取得非空 `IModuleContext`。
- `VERIFY-QUALITY-GATE`：`b-Code-Studio\eng\Test-QualityGate.ps1` 通过，其中必须包含
  「`z-Publish` 未被任何忽略规则覆盖」这一项。
- `VERIFY-HOST-CONTRACT`：`module.manifest.json` 的 `type` 为 `HistoryVulcan.Module`；
  模块工程对宿主运行库的引用为 `Private=false`，`b-Code-Verify` 下三个测试工程为 `Private=true`。
- `VERIFY-VERSION-SINGLE-SOURCE`：全仓 `MinimumHistoryVulcanVersion` 只在
  `AuroraVersion.props` 出现一次，脚本与门禁不得硬编码宿主版本字面量。

**尚未建立**：`VERIFY-COMMAND-PARITY`（迁入首组命令时建立）——用于断言宿主侧退役与本模块
侧接管在同一版本线对齐，`vulcan.command.list` 总数不变、MCP 可见工具数不变。

## 手工冒烟

骨架阶段只需一次：把构建产物放入宿主运行区
（`%AppData%\HistoryVulcan\Modules\HistoryAurora\`）或经 `vulcan.module.install path=` 安装，
确认 `vulcan.module.list` 能列出 HistoryAurora，且宿主日志出现
「HistoryAurora 已装载（骨架，尚未接管前端能力）」。

## 1.21.1 单模块重载动作回归

2026-09-13：Aurora 1.21.0 下 Mercury 5.0.4 热重载复现 6 条 panel Error 和 6 条表格动作未绑定 Warn。原因是 PageInvalidation 直接 ReloadOwnerAsync，仅拉页面，卸载期间清掉的动作台账尚未补回。

ReloadOwner_LoadsActionsBeforeRenderingAndRemovesRetiredActions 使用真实面板建页，覆盖首次加载、动作改名和旧动作撤销；PageInvalidateContractTests 与 ActionDeclarationContractTests 18/18 通过。真实热重载复验在候选安装后执行。

补充：Contracts 全量 372/372 通过；质量门禁通过；本次三份 C# 修改文件格式检查通过。全仓格式检查未通过，报告的是既有 ModuleCommandProbe、PageRegistrar、FloatingDockMouseSmoke 等未修改文件的空白格式问题，未作批量重排。

真实候选验收：run=20260913-184418-HistoryMercury 热重载 Mercury 5.0.4 后，仅 Mercury 实例变化、31 条命令保留；Aurora 1.21.1 保持 63 条。18:44:43 observe 零新增 Error、零模块退化、日志完整、前端实例未变；seq83 mercury.ui.actions 先于 seq85 mercury.ui.describe，原 6 条未声明动作 Error 未复现。
