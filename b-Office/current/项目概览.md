# HistoryAurora 项目概览

## 1.23.1 小更新（进行中）

页面内边距 8 → 12、页面圆角 16 → 20，组件圆角仍 8、同心；页面内组件间距统一为 12（原 tight 8 / normal 12 两档）；
控制面板与控制台工具条的分隔线由渐隐改为与表格相同的实色细线（DEC-043，REQ-UI-122）。

## 1.22 页面新风格（进行中）

控制台不再自带边距，与模块页、自持页共用同一个页面内边距；内边距由 12 收窄到 8。
卡片圆角 16、组件圆角 8，贴角组件与卡片同心（DEC-040，REQ-UI-119）。

独立浮窗退役（DEC-041，REQ-UI-120）：`aurora.ui.float` / `floatstate`、视图菜单「浮动」、布局里保存的浮窗全部删除；
拖页面仍靠拖动途中的临时浮窗，落空即隐藏。验证工程删掉 Smoke 占位工程、浮窗用例与真鼠标冒烟。

## 1.21.0 当前更新

命令按钮有状态：面板、命令开关、表格单元格与行内按钮统一显示沿按钮四周流动的进度边框，防止重复触发，
结束后恢复；面板等待数据刷新完成。响应式栅格修复规则表不能滚动；泳道隐藏滚动条但保留平移。
完整 Contracts 371 项通过。保留 1.20.4 的增量表格接口与常驻信息。

## 定位

OneHistory 体系的**前端应用**，编号 2026-026。名字取自罗马黎明女神 Aurora——本项目是
整个体系对用户可见的那一面：窗口、布局、控制台与主题。

**它是宿主装载的界面模块**（DEC-008 推翻了 DEC-004 的独立 exe 两条）。
启动与显隐走注册指令，发布只有模块包。DEC-004 仍有效的是自持命令注册表与 Core 解冻。

宿主指令与公开 API 以 `../2026-023-HistoryVulcan/b-Office/package/` 为准，本仓不复述。
界面视觉与嵌入页结构见 [HistoryAurora_UI风格与嵌入页面规范.md](HistoryAurora_UI风格与嵌入页面规范.md)。

## 体系位置

OneHistory 体系此前含 5 个项目（Diana 019、Janus 020、Mercury 021、Vulcan 023、Minerva 024）。
HistoryAurora 是第 6 个，也是唯一专职承载界面的项目——它是应用，不是模块形态的界面库。

## 当前状态

**1.20.4。** 表头与控制面板共用圆角底板；隐藏末尾空表头，余宽归末列。表格启用虚拟化回收，复用列与未变行，合并重复刷新；提供版本增量取数与 ApplyDelta 接口，刷新保留内容及常驻状态、数量、更新时间。

**1.20.3。** 顶栏删除这一轮收尾（DEC-035）。**清账**：页签模板只剩一块透明 Border（容器保留，
窗格是 TabControl，内容区靠它产出）；`DelayedDragGesture`、`DockingDragSession.Target` / `RequiresHold`、
`FindDescendants<T>`、令牌 `Aurora.Space.TabStrip` 全删；停靠层七个 partial 收成三个
（对外是什么 / 布局树怎么摆 / 怎么存怎么取），`ISceneDocking` 与 `IDockingService` 并列，
快照尺寸四项的五路 switch 加五个 `Set` 重载换成反射。**形态**：右栏与停靠画布同底、不画竖线（深色下不再过亮）；
Ctrl 标签态的盖板跟着卡片圆角；搜索浮层退役，搜索并进右栏第二行、随输入筛场景与常用页面，
回车打开第一个候选（胶囊点击语义不变）；拖窗口覆盖整条右栏与停靠区画布。

**1.20.2。** 修 1.20.1 真机反馈（DEC-034）：一个位置在几个拖入过的页面之间循环抢占。**一格一页改为结构约束**——
页面进一格前先顶掉原来那一页，登记与恢复不抢位时新来的直接隐藏；等位账、「每页在哪一格」历史、标签组、
`pos=tab` 整条链删除。**命令集改为普通工具页**，不再有文档身份。**顶栏删除**：窗格不画页签行，
双击页签专注、页头拖窗口、控制组搬家一并删掉，控制组只在右栏。Ctrl 标签态去掉 0.3 秒延迟；
右栏胶囊只用来拖，点一下不再打开页面。

**1.20.1。** 修 1.20.0 真机反馈的三件事（DEC-033）：**每一格只留一页**——侧边、底边、主文档区、浮窗一个规矩，
新进来的页顶掉那一格原来那一页（REQ-UI-100）；登记不抢位、占位页被明确隐藏时等位的页回来；模块场景里
本模块的页优先于常驻页（Janus 场景里「图」顶掉同一格的控制台）。Ctrl 标签态改为轮询系统按键，
Aurora 没有键盘焦点也起得来；标签改为「内容隐去、正中一枚页签形标签」。AvalonDock 页签自带的原生拖动卸掉，
顶栏页签平时只换页，不再拖出不收起的悬浮窗。

**1.20.0。** 顶栏退役的第一步（DEC-032）：先转移、不删除顶栏本身。场景改为**只是一份布局**——
不拥有页面，区别只是显隐，`aurora.scene.add / remove` 删除。导航栏搬到右侧，顶部接下窗口控制组
（仍在窗体右上角），下半是「常用页面」胶囊，拖出来经蓝色停靠点嵌进场景。顶栏只留最后进来的一页，
命令集因此解除锚点（仍常驻）。页面拖动改为单独按住 Ctrl 0.3 秒进入的标签态，页签平时只换页；
拖出去没落到停靠点的页隐藏。工具页右侧的 ▼ 菜单与 ✕ 删除。已热装到活宿主（宿主 5.3.0）；
用户试用后反馈仍有多处 bug 与未完成部分，本轮先收尾不修，已知问题记在验证合同 1.20.0 节。

**1.19.0。** 主页面改按场景组织（DEC-031）：每个模块自动得到一个以模块名为 id 的场景，
切场景换掉整个停靠布局，不在场景里的页不出现；控制台与命令集常驻。左侧新增导航栏（常用场景），
搜索浮层由 `aurora.nav.open` 或 Mercury 注册的全局快捷键 `Ctrl+Alt+K` 打开，按使用频次加权。
场景指令 `aurora.scene.*` 对模型开放。顶栏本版未动；`switch` 分支进索引、拆顶栏、模块声明场景
三项未实施（见技术合同「场景与导航」节末尾）。

**1.18.4。** 中央文档区改用 `CenterDocumentPane`：它派生自 `LayoutDocumentPane`，
重新实现 `ILayoutContentSelector.IndexOf`，按 `Children.IndexOf` 取下标。原装那个只认
`LayoutDocument`，对中央区里的工具页一律返回 -1——换页写成「什么都不选」，
AvalonDock 的页签拖拽也在同一个 -1 上起不来。真机症状是「Janus 项目总览拖不动、
`aurora.ui.show` 报成功但页不换，而旁边的命令集一切正常」。一处改对，
换页、拖动、快照恢复同时正确；页签左键仍整个交给 AvalonDock，
Aurora 一个手势都不拦（REQ-UI-083，结清 REQ-UI-082 的挂账）。

AvalonDock 自己建出来的原装窗格由布局差分换掉——否则故障只在「拖动过中央区」的
那台机器上复发。这一条是真机先发现的：模型层修对之后，屏幕上那一格仍然是老窗格。
同样由真机逼出来的还有第三条：模型改对不等于屏幕换页，窗格控件要在同一轮呈现里
按模型对齐一次（`SyncPaneControlSelection`）。

本轮先后走过两条路并撤回：把中央区拆成上下两块（1.18.0，改了结构但把一排页签拆散了），
以及用类处理器只接管工具身份那一半页签（1.18.1，真机当场点不动）。
两次都指向同一件事——病根是那个 -1，不在手势层。

**1.17.6。** 换页交回 AvalonDock：1.17.2/1.17.5 两次「让 Aurora 接管页签左键」都被实测
推翻（判 Handled 会让容器 TabItem 收不到按下，自己写选中要么把窗格选中下标写成 -1、
要么在有隐藏页时错位一格）。现在页签左键既不判 Handled 也不写选中，Aurora 只负责拖，
浮之前先放掉捕获（REQ-UI-082 推翻 REQ-UI-080）。控制台冒烟的清理保留。

**1.17.5。** 点页签终于只是换页：AvalonDock 自带的页签左键实现改用类处理器整条断掉
（隧道阶段的 Handled 拦不住冒泡阶段的就地升发），选中挪到抓取之后；中央页与工具页
两种页签都覆盖（REQ-UI-080）。同时清掉调查期留下的控制台冒烟——847 行的停靠探针
与六条逐次拖动回声（REQ-UI-081）。

**1.17.3 / 1.17.4 已整体撤销**（`656b3f5`）：那两版走的是「中央区不分栏 + 自动并回」，
实测越修越多，回到分叉点改走 A 路线。中央区分栏留下空白窗格的缺陷本轮挂账不修。

**1.17.2。** 中央页签的左键手势收成一个主人：AvalonDock 自带的那条拖拽不再起步，
选中改由 Aurora 自己做；浮出去之前先放掉页签上的鼠标捕获。另加一条兜底——
非空文档区必须有选中内容，杜绝「有页签、没内容」的窗格（REQ-UI-077）。

**1.17.1。** 修停靠层的一处不变量误判：中央区被拆成两个文档区之后（把中央页拖出去
再丢回中央区就会），布局差分每一拍都抛未处理异常，基线建不起来，浮动与存盘一并失败。
中央主文档区改按「第一个非浮窗文档区」单点认定（REQ-UI-076）。

**1.17.0。** 启动期建页改由宿主通知触发：登记 `aurora.host.ready`，宿主装完全部模块后
按命令名通知，界面在钩子里做整轮重拉；原来的「注册表安静 150ms」防抖降为兜底（DEC-030）。
修的是「启动有几率少几页、按钮报未声明」那一类竞态。不抬宿主下限，旧宿主上行为不变。

**1.16.0。** 三层从「测试知道」变成「目录说了算」。`Shell/` 下建了五个层目录
`0-Neutral` / `1-Base` / `2-Components` / `3-HostedPages` / `4-Composition`，根部不再有散文件，
命名空间跟着目录走（`Shell/2-Components/Panels/` → `HistoryAurora.Shell.Components.Panels`）。
层的定义从门禁的两张硬编码表里删掉，改由目录树推导；`csproj` 一层一组 `Include`，
没分层的顶层目录根本不参与编译——这是比测试更早的一道闸。

真正的收获不在目录好看。命名空间带上层名的**那一刻**，编译器一次报出三处反向依赖：
基础层的弹窗 `is ShellWindow` 问装配根要主题、基础层的顶栏在交互控件清单里列着组件层的
`AuroraOptionBox`、页面层两处登记命令时引用装配根的 `FrontendCommandCatalog.Source`。
它们此前全写成短名，靠外层命名空间就近解析，而门禁的正则只认全限定形式——**门禁一直是绿的**。
三处都按方向倒过来修：`IThemedWindow`、`IInteractiveCommandControl` 两个基础层契约，
以及把 `"framework:frontend"` 下沉进中立层。界面行为零变化，`.g.resources` 资源名逐条不变。

**1.15.0。** 页面为显示而自己发起的取数（表格、面板候选）改走宿主安静通道，不再回显控制台——
一次用户动作会连带触发本页全部表格重取，那些回显把真正的结果顶出屏幕。程序回写控件值
（`aurora.ui.panelset`、跟随通道、候选重取）期间不再触发提交动作，避免「模块回写 → 控件当成用户
选择提交回去」的回环。选项框右键菜单不再抢焦点、也不挂 `ContextMenu` 属性，第一次右键即可打开。
宿主下限不动。

**1.14.0。** 控制台内存日志缓冲区新增单调序号、前端实例身份与有界只读快照；
隐藏命令 `aurora.log.snapshot` 在生产端完成级别、来源、关键字、时间和增量过滤，
供 Diana 通过当前进程总线读取，不改变控制台筛选器，也不扫描磁盘日志。消息进入控制台唯一缓冲前
统一脱敏，桌面显示与 AI 读取不会分叉成安全级别不同的两份内容。

**1.13.0。** 页面表格列可用 `cellAction` 把非空单元格变成统一的鼠标/键盘动作入口，动作参数
默认读取被点行字段；`aurora.ui.dialog` 新增返回 `{label,value}` 所选 value 的 `choice` 弹窗；
面板按钮可声明由 Aurora 管控的 Lucide 语义 `refresh-cw` 图标。旧页面和旧面板声明保持兼容。

**1.12.20。** 页面停靠完整布局改由 Aurora 自有的版本化 JSON 快照持久化；快照只记录稳定页面 ID、
分栏与页签拓扑、浮窗几何、自动隐藏和隐藏位置，不再把可回收 ALC 中的 AvalonDock 类型交给
`XmlSerializer`。当前布局原子写入 `%AppData%\HistoryVulcan\layout\layout.v1.json`；损坏、未知版本
或缺页时回退 `layout.placements` 与默认声明。1.12.14 的蓝色停靠覆盖层模板、命中尺寸和只读探针未改。

**1.12.19。** Minerva 属性整备选来源仍打开零件文件夹：转换内容下拉只改选择通道，不提交模块；来源选择指令也不展开页面当前内容。下拉变更立即提交，选择指令展开 `{selection.*}`。

**1.12.18。** 打开 Minerva 仍整窗假死：列宽回写让 ListView 按整页宽度振荡，24px 门槛无效。
分摊改为只跟表格外框走，同一轮最多重启 3 次。

**1.12.17。** 选完文件后主进程 CPU 打满、整窗假死：表格列宽分摊把滚动条显隐（约 17px）当成外部分配，
轮数清零后空转。门槛抬到滚动条之上，连续空转满 5 次强制停；来源提交与表格刷新排到对话框关闭之后的空闲帧。

**1.12.16。** 选文件后整页像死了：来源已经提交进模块，零件表却只在第一次 Loaded 取过空结果，
不再重取。面板动作成功后刷新本页取数；宿主抄过来的界面指令已在界面线程上时直接执行，
不再 `Dispatcher.Invoke` 包一层文件对话框。

**1.12.15。** 选文件后整窗卡住：表格列宽分摊把对话框关闭带来的半像素抖动当成外部分配变化，
把收敛轮数清零后在两档宽度间跳死；来源选择器在打开对话框时失焦，把空路径提交成失败指令，
宿主再去抢控制台。文件/目录对话框恢复进程当前目录，避免后续扫描从 CAD 零件库往上爬。

**1.12.14。** 停靠覆盖层改为 Aurora 自有 `OverlayWindow` 模板：AvalonDock 的外层
`ContentControl` 永远是 40×40 DIP 的命中矩形，方向图形通过独立模板裁剪为 28×28 DIP，
五向与九向星形背景固定为 122/204 DIP。`PART_PreviewBox` 只绘制透明填充的边框，
不再用运行时遍历覆盖窗、修改宽高或缩放几何来压制巨型蓝色控件；停靠命中矩形因此与视觉尺寸
永久分离，热重载和新页面注册不会改变 AvalonDock 的 `GetScreenArea()` 结果。

`DockingDragProbe` 现在是纯只读观测器，不再调用模板应用或任何视觉修复 API；新增源码合同锁定
全部 AvalonDock 模板部件、命中/视觉尺寸边界及探针禁止旁路。保留停靠画刷在主题初始化阶段预热、
页面关闭保持、统一拖动会话与窗口拖动驱动，以及控制面板和描述式页面改动。

**1.9.2。** 六条要求，可以归成三件事：**一根漏接的线、一块重做的版面、
一笔还上的能力欠账**。

一、**命令集空了两版**（REQ-UI-057）。页面表头齐全、控制面板在、一行数据都没有，
日志里一个字也没有。查下来是装配根漏了 `Catalog = _catalog` 这一行——1.9.0 把命令集
与指令详情改成描述式时，取数从视图里的私有状态换成了 `aurora.ui.data`，
而目录会话从来没接上来。它两版没被发现，是因为当时那一路「拿不到会话就返回空表」：
**「没接线」与「真的一条指令都没有」从此在界面上、在日志里完全一样**。
现在改判失败，并且门禁同时守两侧——一条验「没接线会报错」，
一条验「这台机器接上了」，只有前者的话装配根再漏一次仍然全绿。

顺带把这一页重排了（REQ-UI-058）：列改成【域 / 类 / 方法 / 只读 / 说明】——
旧版第一列的完整指令名重复了紧随其后两列的全部内容，而真正有区别的方法名
被挤在最左边一列的末尾。行内按钮那一列删掉，四条动作只进右键菜单，
另外在指令详情页顶上补了一个控制面板放同样三件事。
`name` 这个键**留在行数据里**：去掉一列是版面决定，去掉一个键是断链。

二、**控制面板的行成了一等结构**（REQ-UI-060，破坏性）。此前面板有两套互不相干的排版，
由 `orientation` 二选一，而两套都表达不了「这三个是一行、那一个自己一行」——
竖排里行是 `inline` 的副产品，横排里行是宽度算出来的结果。
现在 `widgets` 换成 `rows`，每行自己说是均布（余量等比放大）还是可变宽度
（余量全给一个）；元素按最窄宽度排，**放得下就不换行，放不下才折**，
而折出来的仍然是同一个声明行——折行是宽度不够时的应对，不是版面结构的变化。
`orientation`、`inline`、`required` 三个字段一并退役（DEC-020），
Janus 与 Mercury 的声明同步改写。

同一版把面板上下那圈「厚边框」收掉（REQ-UI-061）：它不是边框，是底板 Padding 2、
面板内缩 4、控件自带 Margin 3～6 三层叠加。控制台顶栏用的是**同一份底板样式**却只有 2，
于是同一个 `Aurora.Panel.Surface` 在两处看上去完全不是一个东西。

三、**还上 1.8.18 记的那笔能力欠账**（REQ-UI-059，DEC-019）。选择框新增
`optionsSource`：候选来自一条只读指令，参数里可以引用选择通道，通道一变就重取。
命令集的域/类两级联动下拉据此恢复。DEC-018 定的「组件保持克制」不是「永远不加」——
它真正的价值是「界面自己先撞上缺件」，而撞上之后正确的兑现方式是补组件，
不是给某一页开后门。

另外补回一个很早以前有过的能力：**表格列可以拖着换顺序，并且记得住**
（REQ-UI-062）。一律可拖、不加声明；占满剩余宽度的那一列钉在最右——
它不在最后就没有「剩余」可占。

**发布状态**：2026-08-29 19:24 打出 1.9.2 候选包，1.9.1 轮转进 `z-Publish/history/`，
并热重载进运行中的宿主（`vulcan.module.install`）。装载无 attach 失败；
经回环网关实测，命令集取数回 187 行、域/类两级联动在真实注册表上成立——
REQ-UI-057 的真机判据就在这里：目录会话没接上时它现在返回的是失败，不是空表。
剩下四条只能靠肉眼的记在验证合同里。

### 1.9.1

一件事：**弹出层多了一条打开它的路——右键**（REQ-UI-056）。

来源是 Mercury 的扩展坞管理页：顶上常驻着「加入扩展坞」面板（一个输入框加四个按钮），
一周用不到一次，而这一页真正要看的是下面那张条目表。1.9.0 之后页面不滚，
**被它挤掉的高度就是看不见的行**——版面从审美问题变成了功能问题。

弹出层（1.7.0）本来就是为这个造的，但它自带一个按钮，仍然占一行。所以这一版
**不新建组件**：先查了仓里已有的三处右键实现（表格行操作菜单、轮换选项框候选菜单、
控制台复制菜单），三处都是动作菜单、放不进输入框；能复用的是浮层本体，
它缺的只是一个触发方式。于是给 `popup` 加一个 `trigger`，浮层本体一行不动。

施工中撞出的一条实测教训记在 REQ-UI-056：WPF 的 `Popup` 在 `Collapsed` 的父级下
**开不出来，且不抛异常也不触发 `Closed`**——第一稿正是这么写的，表现为"右键完全没反应"，
而除了真的去开一次，没有任何地方会说出这件事。门禁因此必须建窗口并真的打开一次。

### 1.9.0

三件事，其实是同一件：**让界面自己活在自己的规矩里**。

一、界面分三层写进合同并加了门禁（基础层 = 页面外壳，组件层 = 演进层，
页面层 = 自持的那几页）。逐目录比对下来依赖方向**本来就是干净的单向**，
所以这条不是重构，是把既成事实变成约束——没有门禁的分层会在第一次「就这一次」里破掉。

二、工具页不再有页面级滚动，装不下就裁掉，滚动只发生在表格、泳道这类自带视口的组件里。
此前「鼠标停在控制面板上滚不动、挪开又能滚」是两套滚动语义打架，退场的该是外面那层。

三、Aurora 自持的四页全部改成页面描述，与模块页共用同一个渲染器与包边。
用户发现的「Aurora 的页和别的模块间隙不一样」查实是两条通道：自持页写 8、模块页写 12，
而代码里没有任何一处「说了算」的地方可以改它，因为根本不存在那样一处。

第三件的真正意义不在那 4px。此前只有组件测试页是描述式的，另外三页是手搓 WPF，
于是「模块不得自建组件」这条禁令，界面自己是全仓最大的违反者；组件层缺什么，
只有模块作者撞得到、又补不了。逼界面自用之后，**缺件由界面自己先撞上**。

本轮定的原则是「页面适配组件、组件保持克制」，因此净新增组件能力为 **0**：
凡是现有组件表达不了的一律削页面——删掉了域/类两级联动下拉、命令集搜索框的补全、
三页的状态行。逐条记在 REQ-UI-052，不假装没发生。

顺带撞出一条旧缺陷：`view.filterable` / `sortable` / `selection` 在 schema 里声明、
解析器里解析，**全仓没有一处读它**。这正是这套协议要消灭的失败形态，
只不过这次是协议自己犯的。记在 REQ-UI-054，本版不实现也不删。

**1.8.18。** 主窗口打不开、平时又极卡，两件事是同一条链上的两段。

宿主注册表每登记一条命令就 `Changed` 一次，界面每次都去全量拉页面、拉动作、
再问一遍权威目录 `vulcan.command.list`。真机 1.7.0 就测过 34 轮 / 104 次；
2026-08-27 再测两分钟 870 次，而且这些发现排在窗口 Show 前面——窗口等于被自己
的发现风暴堵住了。同日运行区里还留着 `HistoryAurora-rollback-*`，宿主把同名
候选全部跳过，Aurora 根本没装上，`vulcan.app.show` 只回「界面未装载」。

本轮把发现收成「注册表安静后再拉一轮」，权威目录短缓存，通道同样的行不再重复
通知。rollback 目录由宿主扫描跳过（与装载槽位同一条规则）。

**1.8.17。** 通道从「表格单向发布」变成**双向的接线柱**：控制面板的文本框与轮换选项框
可以声明 `channel`，把自己的当前值发上通道（REQ-UI-045）；页面这一层新增 `switch`
容器，按通道值决定显示哪一支（REQ-UI-046）。两件合起来才是「三个页签收进一页」——
Janus 5.4.6 把 Git 文件规则 / 分支历史 / GitHub 三页收进「项目操作」页，用的就是这条。

**它换的是组件，不是页面。** 三支各自还是普通的 `stack` / `table` / `panel`，
只是同一时刻只有一支挂在可视树上。挂树而不是显隐是拿代价换来的：折叠元素照样收
`Loaded`，写成显隐的话三支的取数会在开页那一刻一起打出去，而 Janus 的规则落地状态
那一支每次跑两条 `git ls-files`。

同轮补掉一处**模块无法自救的差异**：走描述协议的模块页此前被直接挂进窗格，
第一个控件贴着窗格边框画，而界面自持的 XAML 页按规范自己写了 `Aurora.Space.Pad`。
描述里没有任何字段能表达内边距——看得见、改不动。现在由装载器统一补上（REQ-UI-047）。

**1.8.16。** 把 1.8.14 弄丢的「表格选中行 → 按钮变可用」接回来，并且**跨页面**可用：
表格用 `channel` 声明把选中行发上一条界面级通道，控制面板用 `follows` 跟随取值、
用 `enabledWhen` 按有无选中启停，动作参数用 `{selection.<通道>.<列>}` 取选中行
（REQ-UI-041）。落点从页内节点 id 换成界面级通道，是"能跨页"的全部原因——
Janus 的项目表在中央页、操作面板在左侧页，页内 id 到不了对面。

同轮还有一件：取数不再只在建页时跑一次——参数可以引用选中行，通道一变自动重取，
也能被 `aurora.ui.refreshdata` 显式刷新（REQ-UI-044）。没有它，「刷新」按钮只能把
指令打到控制台，而依赖选中的表换一次选中就成了过期数据。

同轮两件：`stack` 改成按行分档的 Grid，表格与泳道图拿剩余尺寸，
修掉「表格撑满整页且滚不动」（REQ-UI-042）；面板按钮可声明 `inline` 与前一个控件同行，
一行因此是「左标签 / 中控件 / 右按钮」（REQ-UI-043）。

**外加一处既有缺陷**：动作占位符正则是 `\w+`，不含连字符，
`{project-name}` 这类控件 id 从来没被识别为占位符，原样上了总线。写用例时才撞出来。

**1.7.5。** 加了一套**真实鼠标**冒烟（`FloatingDockMouseSmoke`，默认不跑，
`AURORA_MOUSE_SMOKE=1` 开启），专门覆盖"浮窗拖回来合并成页签组"这条链路——
它此前在门禁里驱动不了，改了三轮全靠推理，推错过两次。
**实测全绿、连跑两次稳定**：1.7.4 那个修法（拖动前放掉 Win32 鼠标捕获）成立。
同时给 `TryDragWindow` 加了进出日志，真机上拖不动时先看这两条在不在。

**1.7.4。** 浮窗叠不回去的第三轮，前两轮的判断已作废。反编译确认 AvalonDock 的
`FilterMessage` 只看 WM_SYSCOMMAND(仅最大化/还原)/WM_LBUTTONUP/**WM_MOVING**/WM_EXITSIZEMOVE，
不看非客户区消息；建 DragService（那组蓝色指示）的是 `WM_MOVING → UpdateDragPosition`，
而且反射直调验证过它能建起来——**停靠机制本身是好的**。
真正的缺口是 `DragMove()` 之前没放掉 Win32 鼠标捕获：捕获还在，系统的移动循环就不启动，
`DragMove()` 静默地什么都不做，一条 WM_MOVING 都不发。
`CaptionHeight` 回到 0，`WM_NCLBUTTONDOWN` 的做法撤销。

**1.7.3。** 双击标题聚焦的 `NotSupportedException` 找到根因并修掉：
`XmlLayoutSerializer` 走 `XmlSerializer`，而 AvalonDock 随模块包装进宿主的
**可回收 AssemblyLoadContext**（Aurora 是 `pinned:false`），对其中的类型做 XML 序列化
直接报「非可回收程序集不能引用可回收程序集」。门禁里复现不出来——测试进程把 AvalonDock
装在默认上下文里。聚焦与还原改为留住那棵 `LayoutRoot` 的引用，不再经 XML。
**同一根因还解释了另一件事：布局持久化一直是坏的**（`layout` 目录始终是空的），
那一条本轮未修。

**1.7.2。** 浮窗拖动交回标题栏拖动——1.7.1 只抬起了 `CaptionHeight`（前提条件），
而 Aurora 自己接管了浮窗页头的拖动、仍走 `Window.DragMove()`，
AvalonDock 要的 `WM_NCLBUTTONDOWN`/`HTCAPTION` 始终没发出去，所以装上去仍然叠不回去。
现在按 AvalonDock 自己 `AttachDrag` 的那一招把消息发出去。**只能真机确认手感。**
同轮修掉命令集那条横向滚动条（星号列改以滚动视口为准）与模块管理被挤没的描述列
（定宽列各收一档 + 截断的字可悬停看全）。

**1.7.1（已部署，2026-08-25 15:28）。** 修停靠：浮窗叠得回去、双击聚焦的异常可定位、
自探测的错误消掉。真机已确认最后一条——`✗ 未知指令: aurora.ui.actions` 不再出现，
顶栏错误计数归零。前两条要人工试手感：拖放在自动化里驱动不了。

- **浮窗叠不回去**（REQ-UI-019）：`WindowChrome.CaptionHeight="0"` 让浮窗永远收不到
  `WM_NCLBUTTONDOWN/HTCAPTION`，AvalonDock 因此不创建 DragService。
  拖出走的是主窗体里的另一条路，所以只坏了一半。CaptionHeight 只管命中测试、
  一个像素都不画，与"去掉第二条标题行"是两件事——当初被当成了一件。
- **`✗ 未知指令: aurora.ui.actions`**（REQ-UI-020）：`aurora.ui.actions` 是查询命令，
  与协议槽位同名，探测把界面自己当成了声明动作的模块。现在排除自己的域。
- **`aurora.ui.max` 的 NotSupportedException**（REQ-UI-021/022）：**未能复现**。
  本轮让它下次可定位（异常全文进本端日志，总线只外发类型名），
  并把最可疑的一条路径改掉——组件构造时按 URI 取控件字典改成按线程缓存且不抛。
  推断，不是已证实的修复。

**1.7.0。** 补组件 + 找回命令集 + 收停靠面。

- **行操作**（REQ-UI-011）：表格的 `rowActions` 一份声明出两个出口——行内按钮与右键菜单。
  触发时带的是**被操作那一行**，不是选中行。1.6.0 只能"先选行、再下去点按钮"。
- **弹出层 / 补全输入框 / 响应式栅格**（REQ-UI-012 / 013 / 015）：
  对着 Mercury《前端组件缺失需求书》的 C3 / C4 / C5 逐条交付，五条缺件全部销账。
- **命令集与指令详情找回**（REQ-UI-014、DEC-015）：这两页原先由 Mercury 的命令工作台挂上来，
  宿主 5.0 拆掉界面 SDK 后**整页消失**，控制台 Tab 补全一并成了死路。
  现在由 Aurora 自建目录会话（本地注册表 ∪ 宿主 `vulcan.command.list`）——
  目录是界面的基础设施，不该取决于哪个模块在不在场。补全随之恢复。
- **停靠系统收成 internal**（REQ-UI-016、DEC-014）：`HistoryAurora.Shell.Docking`
  零导出，连带 `ShellWindow` / `ShellConfig` / `BuiltinCommands` 等；
  两条失效的 `InternalsVisibleTo`（`HistoryVulcan.Tests`、`HistoryMercury`）删除。
- **组件区改为公共区**（REQ-UI-017、DEC-016）：任何项目都可以直接来加组件，
  五条门槛写在组件清单里。收骨架与放组件同一轮做——正因为骨架收紧了，组件区才敢放开。
- **两处真机缺陷已修**：`aurora.ui.invalidate` 的 owner/域混用（REQ-UI-018，需求书 D1）；
  表格最右侧那"多出来的一列"（其实是 `GridView` 的 `Role=Padding` 表头套了列样式）。
  模块管理页去掉「暴露」列，星号的描述列不再被挤成"前端…"。

**已部署**（2026-08-25 14:36）：1.7.0 在真机宿主（5.1.0）里进程内热重载，PID 不变。
命令集回到中央主区并列出 120 条、行操作按钮在位；指令详情停在右侧；模块管理已无「暴露」列；
表格最右侧那一列空的不再出现。`Contracts` 197 条全绿。

真机上另发现四条待修（见验证合同 `VERIFY-DEPLOY-170`）：描述列仍被挤窄、
命令集表格有横向滚动条、一次热重载触发 34 轮界面发现（1.6.0 即存在）、
模块的上行通知会撞上 Aurora 重新注册命令前的窗口期。
弹出层 / 补全 / 栅格 / 右键菜单仍未在真机上出现过——它们要有模块的页面描述才会渲染。

### 1.6.0（已部署）

组件建设轮：表格组件 `AuroraTable`（REQ-UI-007、DEC-011，全仓唯一实现，外观封在组件里）、
面板组件收成三种小组件（REQ-UI-008）、动作声明协议让按钮绑 id 不绑指令名
（REQ-UI-009、DEC-012）、泳道图从 Janus 收回 Aurora（REQ-UI-010、DEC-013）、
对齐宿主 5.1（REQ-HOST-511，公开 API 纯增量）。
1.6.0 已在真机宿主（5.1.0）里热重载并验证。

**仍欠的两笔**：宿主 `ModuleMeta.Attached` / `AttachFailures` 现在可用，
模块管理页应补上"Attach 失败"一列（当前"模块坏了却看不出来"的直接补救）；
Janus 仓的泳道实现待删除（DEC-013 边界，两边实现仍并存）。

1.5.2 起的既有状态：对齐宿主 5.0：不再实现已删除的 `IUiModule` / `IShellUiProvider`，
停靠与目录会话契约改由本模块自持；界面在 `Attach` 里启动，卸载走 `IDisposable`。
`AuroraDialogWindow` / `aurora.ui.dialog` 自己合并主题字典，不依赖 `Application.Current`。
Janus 5.2.0 已把页面弹窗改走该命令。manifest 不再 `pinned`：界面随模块热重载拆掉重建，
依赖已刷新的 Vulcan 宿主（先拆旧界面再装新包）。

模块开发走宿主命令行 `HistoryVulcan.exe --cli vulcan.dev.start` / `submit` / `finish`，在 `F:\ai工作区` 工作区改，不直接改 Clio 主树。

Aurora 同时在 Vulcan 的模块注册表里有平级身份（DEC-007）：`b-Code-Studio/Module`
产出 `HistoryAurora.Module.dll`，`MainClassType = null`——只承担注册表条目，不经模块路径
注册命令（前端命令要求 UI 线程与活动窗口，由应用进程自持并上报，来源标记 `frontend:*`）。

**尚未完成**：`Aurora.Segment.Toggle`（P0，是解除「模块不得自建组件」禁令的前置）、
组件检查器（REQ-UI-002）、页面注册改走模块间调用（C 阶段）。

## 要解决的问题

HistoryVulcan 3.13.0 实测：`vulcan` 域 81 条命令中 39 条由前端进程注册
（`frontend:HistoryVulcan.Frontend`），构成为 ui 21、log 10、app 4、command 4。
这 39 条的 `PolicyVisible` 全为 false——**零条投影为 MCP 工具**，却占着服务进程注册表
近一半条目，并参与每次 schema 导出与目录快照。

把它们迁到本模块后，宿主可以只保留指令总线与 MCP，而 MCP 工具面一个不少。

## 迁移策略

**不再按命令分组零敲碎打**，改为整体切出——`HistoryVulcan.Shell` 12,712 行连同 `App` 的
前端半边一次迁入，产出 `HistoryAurora.exe`。详见
[前端切出迁移方案.md](前端切出迁移方案.md)（REQ-A1..A4 / B1..B2 / C 阶段）。

按命令分组迁移的方案（曾计划 `log.*` 先行）已被实测否决：前端命令依赖 `ShellWindow`、
`ConsoleView`（21 处引用）、UI 线程，而模块命令只落服务进程，路径根本不通。
且 `log.*` 并非"耦合最浅"——它是全组里最深的。

## 尚未决定

- B 阶段页面描述模型的具体形态：区域与组件的粒度、数据绑定如何表达
- 布局快照后续版本的迁移策略；1.12.20 已确定由 Aurora 自持并复用现有 `layout/` 目录
- 模块 UI 在 C 阶段收口后，模块如何声明"我要一个页面"——清单字段还是命令

1.21.1 候选：修复单模块页面 invalidate 在动作台账未就绪时建页，保留 1.21.0 状态按钮与 1.20.4 表格更新能力。

1.22.0 候选：适配 HistoryVulcan 5.4——经 RegisterFrontend 登记为宿主唯一前端，设置、路径、历史与选择状态迁回 Aurora 自持，最低宿主 5.4.0（DEC-039）。

1.23.0 候选：控制台只显示宿主唯一日志（`IModuleContext.Log`），任何模块经宿主执行的指令过程都进控制台，最低宿主 5.5.0（DEC-042）。
